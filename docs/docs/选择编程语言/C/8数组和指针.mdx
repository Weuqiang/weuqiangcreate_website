---
sidebar_position: 8
title: 
---




## 

****````



- 
- 
- 
- 0



` [];`

```c showLineNumbers
// int 
// ages 
// [10]   
int ages[10];
// ages, int [10]

int scores[5];     // 5
double temps[30];    // 30
char name[20];     // 20
```


### 

```c showLineNumbers
// , { },
// 
int ages[3] = {4, 6, 9};

// ,,
int days[] = {31,28,31,30,31};  // 5

// 0
int arr[5] = {1, 2};     // [1, 2, 0, 0, 0]

// ,
int nums[5] = {[0] = 1,[1] = 2}; // [1, 2, 0, 0, 0]


// 
int nums[3];

nums[0] = 1;
nums[1] = 2;
nums[2] = 3;

int ages[3]; // 
ages = {4, 6, 9}; // 
```

,<Highlight>,</Highlight>,


:::info

[]



```c showLineNumbers
#include <stdio.h>

int main() {
    int ages1['A'] = {19, 22, 33};// 'A'  65
    printf("ages1[0] = %d\n", ages1[0]);

    int ages2[5 + 5] = {19, 22, 33};
    printf("ages2[0] = %d\n", ages2[0]);

    int ages3['A' + 5] = {19, 22, 33};// 'A' + 5 70
    printf("ages3[0] = %d\n", ages3[0]);

    printf("sizeof(ages3) = %lu\n", sizeof(ages3));//  70 * 4 = 280
}

```
:::

### 

 0 -1 



```c showLineNumbers
int nums[3] = {10, 20, 30};
nums[0] = 15;       // 
int x = nums[2];     // 

// 
int arr[3];
arr[5] = 10;  // 
```

### 



```c showLineNumbers
#include <stdio.h>
int main() {
    
    int ages[4] = {11, 22, 33, 44};

    // ()
    for (int i = 0; i < 4; i++) {
        printf("ages[%d] = %d\n", i, ages[i]);
    }

    // ()
    for (int i = 3; i >=0; i--) {
        printf("ages[%d] = %d\n", i, ages[i]);
    }

    return 0;
    }
```


### 



 =  x 

 `sizeof` 

```c showLineNumbers
#include <stdio.h>
int main() {
    int ages[4] = {19, 22, 33};
    int length = sizeof(ages) / sizeof(ages[0]);
    printf("length = %d\n", length); 
    return 0;
}

```

- ****
- 
- 

```c showLineNumbers
#include <stdio.h>
int main()
{
    char cs[] = {'l','n','j'}; // 999897
    // 
    printf("cs = %p\n", &cs);       // cs = 97
    // 
    // , 
    printf("cs[0] = %p\n", &cs[0]); // cs[0] = 97
    printf("cs[1] = %p\n", &cs[1]); // cs[1] = 98
    printf("cs[2] = %p\n", &cs[2]); // cs[2] = 99
    return 0;
}
```

:::info
 1-8 MB MB



//



```
 = 
  - 
  - 
  - 

 = 
  - 
  - 
  - 
```

|  | Stack | Heap |
|------|------------|-----------|
| **** |  |  |
| **** |  |  |
| **** |  ×  |  |
| **** |  |  |
| **** |  |  |
| **** |  |  |
:::

### 


- 
- 


- 

```c showLineNumbers
void change(int val)// int val = number
{
    val = 55;
}
int main(int argc, const char * argv[])
{
    int ages[3] = {1, 5, 8};
    printf("ages[0] = %d", ages[0]);// 1
    change(ages[0]);
    printf("ages[0] = %d", ages[0]);// 1
}
```

> - 



### 

-  C """"
- 
- 

```c showLineNumbers
void change2(int array[3])// int array = 0ffd1
{
    array[0] = 88;
}
int main(int argc, const char * argv[])
{
    int ages[3] = {1, 5, 8};
    printf("ages[0] = %d", ages[0]);// 1
    change(ages);
    printf("ages[0] = %d", ages[0]);// 88
}
```


### 

- ,

```c showLineNumbers
void change(int array[])
{
    array[0] = 88;
}
```

- 

```c showLineNumbers
void prtArray(double array[3]) // 
{
    for (int i = 0; i < 3; i++) {
        printf("array[%d], %f", i, array[i]);
    }
}
int main(int argc, const char * argv[])
{
    int ages[3] = {1, 5, 8};
    prtArray(ages[0]);
}
```

- 

```c showLineNumbers
void printArray(int array[])
{
    printf("printArray size = %lu\n", sizeof(array)); // 8
    int length = sizeof(array)/ sizeof(int); // 2
    printf("length = %d", length);
}
```


## 

### 

-  ,. 
- :

```c showLineNumbers
int a[2][3] = { {80,75,92}, {61,65,71}};
```

- :

*  [][]
* ""
* ""


### 

- :

* 
* 

- 

```c showLineNumbers
int a[2][3]={ {80,75,92}, {61,65,71}};
```

- 

```c showLineNumbers
int a[2][3];
a[0][0] = 80;
a[0][1] = 75;
a[0][2] = 92;
a[1][0] = 61;
a[1][1] = 65;
a[1][2] = 71;
```

- 

```c showLineNumbers
int a[2][3]={ {80,75,92}, {61,65,71}};
```

- 

```c showLineNumbers
int a[2][3]={ 80,75,92,61,65,71};
```

- 

* ,

```c showLineNumbers
int a[][3]={{1,2,3},{4,5,6}};int a[][3]={1,2,3,4,5,6};
```

- ,

```c showLineNumbers
int a[][3]={{1},{4,5}};int a[][3]={1,2,3,4};
```

> - : 
>   `int a[2][] = {1, 2, 3, 4, 5, 6};  // `
>    1  1  12  1  1234  123456  1 

- 

```c showLineNumbers
int a[2][3]={[1][2]=10};int a[2][3]={[1]={1,2,3}}
```

### 



### 

### 

-  a[3][4],,:

*  4 ,: a[0]  a[0][0],a[0][1],a[0][2],a[0][3]
* , 

- 

```c showLineNumbers
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    printf("%c", cs[0][0]);// [0], [0]
```

```c showLineNumbers
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    for (int i = 0; i < 2; i++) { // 
        // i
        for (int j = 0; j < 3; j++) {// 
            printf("%c", cs[i][j]);
        }
        printf("\n");
    }
```

> : ,a[0],a[1],a[2],,


### 

- 

* 
* , 
* 

```c showLineNumbers
#include <stdio.h>
int main()
{
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    // cs == &cs == &cs[0] == &cs[0][0]
    printf("cs = %p\n", cs);                // 0060FEAA
    printf("&cs = %p\n", &cs);              // 0060FEAA
    printf("&cs[0] = %p\n", &cs[0]);        // 0060FEAA
    printf("&cs[0][0] = %p\n", &cs[0][0]);  // 0060FEAA
    return 0;
}
```

||||||
|---|---|---|---|---|
|cs|cs[1]|cs[1][2]||f|
|cs|cs[1]|cs[1][1]|...|e|
|cs|cs[1]|cs[1][0]|...|d|
|cs|cs[0]|cs[0][2]|...|c|
|cs|cs[0]|cs[0][1]|...|b|
|cs|cs[0]|cs[0][0]||a|


### 

- 

```c showLineNumbers
#include <stdio.h>

// , 
// 
void change(char ch){
    ch = 'n';
}
int main()
{
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    printf("cs[0][0] = %c\n", cs[0][0]); // a
    change(cs[0][0]);
    printf("cs[0][0] = %c\n", cs[0][0]); // a
    return 0;
}
```

- 

```c showLineNumbers
#include <stdio.h>

// , 
// 
void change(char ch[]){
    ch[0] = 'n';
}
int main()
{
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    printf("cs[0][0] = %c\n", cs[0][0]); // a
    change(cs[0]);
    printf("cs[0][0] = %c\n", cs[0][0]); // n
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>

// , 
// 
void change(char ch[][3]){
    ch[0][0] = 'n';
}
int main()
{
    char cs[2][3] = {
        {'a', 'b', 'c'},
        {'d', 'e', 'f'}
    };
    printf("cs[0][0] = %c\n", cs[0][0]); // a
    change(cs);
    printf("cs[0][0] = %c\n", cs[0][0]); // n
    return 0;
}
```

- 

```c showLineNumbers
void test(char cs[2][]) // 
{
    printf("\n");
}

void test(char cs[2][3]) // 
{
    printf("\n");
}

void test(char cs[][3]) // 
{
    printf("\n");
}
```

- 

```c showLineNumbers
void test(char cs[2][3])
{
    int row = sizeof(cs); // 48
    printf("row = %zu\n", row);
}
```

- 

```c showLineNumbers
void test(char cs[2][3])
{
    size_t col = sizeof(cs[0]); // 3
    printf("col = %zd\n", col);
}
```


## 

- 

* :
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/9485373b7cb46f41f6c996303419ba4c.png)
* :
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/e46d975be9e95178136726209b2c237f.png)

- 

* , 
* , 

- :

* """"

```c showLineNumbers
int num = 6; // 4
//num: 0ff06

char c = 'a'; // 1
//c:0ff05
```

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/13eb965757dd130d813a953506335b89.png)

---

## 

- ,, .
- “”

- ()
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/540000371e0da0fb105248642ef9db24.png)

## 

-  C ,, , 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/02442ae9685e56146470369e56ca18ab.png)
- :

```bash showLineNumbers
    int age;// 
    num = 10;
    int *pnAge; // 
    pnAge = &age;
```

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/8b7b4d0976ad6e192e3ad6e26282fa8a.png)

## 

- :

* ,;
* ;
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/c051703dcae8535c264ef47c20881b53.png)

- :

```c showLineNumbers
char ch = 'a';
char *p; // 
p = &ch;
int num = 666;
int *q; // 
q = &num;
```

> - ,\*
> - 
> - 

---

## 

- :

* 

```c showLineNumbers
int a = 5;
int *p = &a;
```

- 

```c showLineNumbers
int a = 5;
int *p;
p=&a;
```

-  NULL

```c showLineNumbers
int *p=NULL;
int *q=0;
```

- :

* , 

```c showLineNumbers
int *p;
p =  250; // 
```

- ,“\*”

```c showLineNumbers
int *p;
*p=&a; //
```

- :

* 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/d4c9b29d8ddb808d1a12252fdcdf538a.png)

- 

```c showLineNumbers
int a = 5;
int *p = &a;
int b = 10;
p = &b; // 
```

- ,

* 
* 
* 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/e38cdad483acddca8cfcd85e7ac0be78.png)

## 

- C &:

* &;

- C \*

*  \* ,

```c showLineNumbers
int *p=NULL; // 
```

-  \*,

```c showLineNumbers
int a = 5;
int *p = &a;
printf("a = %d", *p); // 
```

## 

- ,
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/a95f575889111ad8d64cc2c1aa8ecb1e.png)

- , ,

*  int  4  char  1  double  8 
*  4  1  8 
* 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/af1fd05a523d9ba4e196e326f537589b.png)

---

## 

- ,  “”

```c showLineNumbers
    char c = 'a';
    char *cp;
    cp = &c;
    char **cp2;
    cp2 = &cp;
    printf("c = %c", **cp2);
```

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/af331163ce03df7573810fc05ceb03c6.png)

- 

```c showLineNumbers
int ***m1;  //***m1
int *****m2; //*****m2
```

---


## 

- 

* ,,, 
* , 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/69b98c6401a04bf8fce52a78c80b9565.png)

```c showLineNumbers
    printf(“%p %p”, &(a[0]), a); //:0x1100, 0x1100
```

> - :  a ,
> - “p=a;”“ a  p”,“ a  p”

##

## 

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/13c0dd997ce619b1249220ce09610ad4.png)

```c showLineNumbers
    int main (void)
{
      int a[5] = {2, 4, 6, 8, 22};
      int *p;
      // p = &(a[0]);
      p = a;
      printf(“%d %d\n”,a[0],*p); // : 2, 2
}

```

- ,:

* (++=), p+1
* (--=), p-1
* , p++,++p
* , p--,--p

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/6c563fff4c62724055337604c2d31623.png)

> -  p , p+1``,p-1`` 

- : ,:

* ,  a[i]
* , \*(p+i)

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/e00312fbc775c04e2a449d7d8d473eb9.png)

- :

* 

```c showLineNumbers
  int x[10];
	x++;  //
	int* p = x;
	p++; //
```

##

## 

- 

* 

```c showLineNumbers
char string[]=”I love lnj!”;
printf("%s\n",string);
```

- 

```c showLineNumbers
// 0, 0
char *str = "abc"
```

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/cb21fae540cb3290bbc41e7e223d3a41.png)

- 

* 

```c showLineNumbers
har *str = "lnj";
for(int i = 0; i < strlen(str);i++)
{
  printf("%c-", *(str+i)); // :l-n-j
}
```

- - 

```c showLineNumbers
//   + ,,
//   + ,,,
char *str = "lnj";
*(str+2) = 'y'; // 
```

- - 

```c showLineNumbers
// :str,
// str 
char *str;
scanf("%s", str);
```

## 

- 

* 
* 
* 

- 

* : ` (*)(1, 2, ...);`

```c showLineNumbers
    int sum(int a,int b)
    {
        return a + b;
    }

    int (*p)(int,int);
    p = sum;
```

- 

* 1
* 2
* 3\*
* 4

- 

* 
* 

- :

* ( p++)
* "()",, 

## 

- 
- , 
- ,,;;;;
- , ,C ——“(structure)”“”

---

## 

- ,  C ,  C , 
- :

```c showLineNumbers
struct{
     11;
     22;
     ……
     nn;
 };
```

- :

```c showLineNumbers
struct Student {
    char *name; // 
    int age; // 
    float height; // 
};
```

---

## 

- , 
- : `struct  ;`
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/a19ef99bede1b3b0d689959881390bd0.png)

- 

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };

 struct Student stu;
```

- 

```c showLineNumbers
struct Student {
    char *name;
    int age;
} stu;
```

- 

```c showLineNumbers
struct {
    char *name;
    int age;
} stu;
```

> - ,,,

---

## 

- `.`

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };
 struct Student stu;
 // stuage
 stu.age = 27;
 printf("age = %d", stu.age);
```

---

## 

- 

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };
struct Student stu = {“lnj", 27};
```

- 

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };
struct Student stu = {.age = 35, .name = “lnj"};
```

- 

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };
 struct Student stu;
stu.name = "lnj";
stu.age = 35;
```

- 

```c showLineNumbers
struct Student {
     char *name;
     int age;
 };
struct Student stu;
stu2 = (struct Student){"lnj", 35};
```

---

## 

- 

* ,  return 

- 

* ,

```c showLineNumbers
//,
struct Person{
    int age;
    char *name;
};

int main(int argc, const char * argv[])
{
    //Person,
    //,“}”
    struct Person{
        int age;
    };
    // 
    struct Person pp;
    pp.age = 50;
    pp.name = "zbz";

    test();
    return 0;
}

void test() {

    //p
    struct Person p = {10,"sb"};
    printf("%d,%s\n",p.age,p.name);
}
```

## 

- , 
- : `struct  []`

```c showLineNumbers
struct Student {
    char *name;
    int age;
};
struct Student stu[2];
```

- , 
  - 

```c showLineNumbers
struct Student {
    char *name;
    int age;
};
struct Student stu[2] = {{"lnj", 35},{"zs", 18}};
```

- - 

```c showLineNumbers
struct Student {
    char *name;
    int age;
};
struct Student stu[2];
stu[0] = {"lnj", 35};
stu[1] = {"zs", 18};
```

## 

- ,
- : `struct  *`
- :

```c showLineNumbers
      // 
      struct Student {
          char *name;
          int age;
      };

     // 
     struct Student stu = {“lnj", 18};

     // 
     struct Student *p;

    // stu
    p = &stu;

     /*
      3
      */
     // 1.
     printf("name=%s, age = %d \n", stu.name, stu.age);

     // 2(*).
     printf("name=%s, age = %d \n", (*p).name, (*p).age);

     // 3->
     printf("name=%s, age = %d \n", p->name, p->age);

     return 0;
 }
```

- , 

* (\*).
* ->()

> - (pstu),“.”“”
> -  pstu.num (pstu.num),,

## 

- , 
- , 
- ()

> +,  k ,, k (alignment modulus)
>
> - ,, 8 , 8 ,  double  8 , double ,, 8  

## 

```c showLineNumbers
    struct Person{
        int age; // 4
        char ch; // 1
        double score; // 8
    };
    struct Person p;
    printf("sizeof = %i\n", sizeof(p)); // 16
```

-  score,  8 ,  8 
-  8  age4 , ch1 ,  3 
-  score ,  3 ,  8 
-  8 ,  p  16 

```c showLineNumbers
    struct Person{
        int age; // 4
        double score; // 8
        char ch; // 1
    };
    struct Person p;
    printf("sizeof = %i\n", sizeof(p)); // 24
```

-  score,  8 ,  8 
-  8  age4 , 4 
-  score ,  4 ,  8 
-  8  score,  0 
-  ch , ,  8 
-  3  8 ,  p  24 

## 

- ,

```c showLineNumbers
struct Date{
     int month;
     int day;
     int year;
}
struct  stu{
     int num;
    char *name;
    char sex;
    struct Date birthday;
    Float score;
}
```

-  stu  Date 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/de4aedd768012888dc7b2e70a744c86d.png)

> - :
> - ,
>
> ```c showLineNumbers
> struct Student {
>  int age;
>  struct Student stu;
> };
> ```

- 
  - "."

```c showLineNumbers
struct Date {
       int year;
       int month;
       int day;
  };

  struct Student {
      char *name;
      struct Date birthday;
 };

 struct Student stu;
 stu.birthday.year = 1986;
 stu.birthday.month = 9;
 stu.birthday.day = 10;
```

---

## 

- , , 

```c showLineNumbers
    struct Person{
        char *name;
        int age;
    };
    struct Person p1 = {"lnj", 35};
    struct Person p2;
    p2 = p1;
    p2.name = "zs"; // p2p1
    printf("p1.name = %s\n", p1.name); // lnj
    printf("p2.name = %s\n", p2.name); //  zs
```

- , , 

```c showLineNumbers
#include <stdio.h>

struct Person{
    char *name;
    int age;
};

void test(struct Person per);

int main()
{
    struct Person p1 = {"lnj", 35};
    printf("p1.name = %s\n", p1.name); // lnj
    test(p1);
    printf("p1.name = %s\n", p1.name); // lnj
    return 0;
}
void test(struct Person per){
    per.name = "zs";
}
```

## 

- , , 
- , , 
- :

```c showLineNumbers
union {
     ;
     ;
    ...   ....
};
```

- :

```c showLineNumbers
union  ;
```

- : , , 
- :

```c showLineNumbers
    union Test{
        int age;
        char ch;
    };
    union Test t;
    printf("sizeof(p) = %i\n", sizeof(t));

    t.age = 33;
    printf("t.age = %i\n", t.age); // 33
    t.ch = 'a';
    printf("t.ch = %c\n", t.ch); // a
    printf("t.age = %i\n", t.age); // 97
```

- 
  - 1
  - 2 2  +3


## 

- C ,,,,
- : `#define () `

```c showLineNumbers
// 12average
#define average(a, b) (a+b)/2

int main ()
  {
  // 4int a = (10 + 4)/2;
    int a = average(10, 4);
  // 7
  // 
    printf("%d", a);
    return 0;
 }
```

- **:**
- 1).

```c showLineNumbers
#define average (a, b) (a+b)/2

 int main ()
 {
     int a = average(10, 4);
     return 0;
 }
1average(a, b)5
int a = (a, b) (a+b)/2(10, 4);

```

- 2)

```c showLineNumbers
#include <stdio.h>
  // D(a)a2
  #define D(a) 2*a
  // 

  int main ()
  {
  // int b = 2*3+4;103#define D(a) 2*(a)a7int b = 2*(3+4);14

     int b = D(3+4);
     printf("%d", b);
     return 0;
 }
```

- 3)

```c showLineNumbers
#include <stdio.h>
// P(a)a
#define Pow(a) (a) * (a) // 

int main(int argc, const char * argv[])      {
// :int b = (10) * (10) / (2) * (2);
// int b = 10 * (10 / 2) * 2;b:100
      int b = Pow(10) / Pow(2);

      printf("%d", b);
      return 0;
}
```

```c showLineNumbers
#include <stdio.h>
// 
#define Pow(a) ( (a) * (a) )

int main(int argc, const char * argv[])      {
// :int b = ( (10) * (10) ) / ( (2) * (2) );
// int b = (10 * 10) / (2 *2);25
      int b = Pow(10) / Pow(2);

      printf("%d", b);
      return 0;
}
```

## typedef 

- C  􏰀 ,,“”
- : `typedef  ;`

* ,,
*  typedef ,, typedef  ,
  ##typedef 

- 

```c showLineNumbers
typedef int INTEGER
INTEGER a; //  int a;
```

- 

```c showLineNumbers
typedef int Integer;

typedef Integer MyInteger;

```

-  typedef , ,

- 

```c showLineNumbers
typedef char NAME[20]; // NAME,20NAME ,
NAME a; //  char a[20];
```

- 

* :

```c showLineNumbers
 struct Person{
    int age;
    char *name;
};

typedef struct Person PersonType;
```

    + :

```c showLineNumbers
typedef struct Person{
    int age;
    char *name;
} PersonType;
```

    + :

```c showLineNumbers
typedef struct {
    int age;
    char *name;
} PersonType;
```

- 

* :

```c showLineNumbers
enum Sex{
    SexMan,
    SexWoman,
    SexOther
};
typedef enum Sex SexType;
```

    + :

```c showLineNumbers
typedef enum Sex{
    SexMan,
    SexWoman,
    SexOther
} SexType;
```

    + :

```c showLineNumbers
typedef enum{
    SexMan,
    SexWoman,
    SexOther
} SexType;
```

- 

* typedef 

```c showLineNumbers
 // 
  typedef struct {
      float x;
      float y;
  } Point;

 // 
 typedef Point *PP;

```

- typedef 

```c showLineNumbers
// sumab
  int sum(int a, int b) {
      int c = a + b;
      printf("%d + %d = %d", a, b, c);
      return c;
 }
 typedef int (*MySum)(int, int);

// sump
 MySum p = sum;
```

##  typedef 

- 

* 
* 1> 
* 2> 

- typedef #define 

*  typedef 
* , 
* typedef ,,  

```c showLineNumbers
typedef char *String;
int main(int argc, const char * argv[])
{
     String str = "This is a string!";
     return 0;
}


#define String char *
int main(int argc, const char * argv[])
{
    String str = "This is a string!";
     return 0;
}
```

```c showLineNumbers
typedef char *String1; // char *String1
#define String2 char * // String2
int main(int argc, const char * argv[]) {
        /*
        str1str2str3char
        String1char *:
        char *str1;
        char *str2;
        */
      String1 str1, str2;
        /*
        , 
        char *str3, str4;
        *, 
        char *str3;
        char str4;
        */
      String2 str3, str4;
      return 0;
}
```

