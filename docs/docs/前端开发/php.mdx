---
sidebarDepth: 4
---

:::danger
PHP
:::


****

``` php
<?php
$value = 'abc';
echo "$value"; // abc
echo '$value'; // $value
echo ''.$value; // abc
?>
```



****

``` php
$arr = array('a', 'b', 'c');
$arr1 = array("a"=>"aaa", "b"=>"bbb", "c"=>"ccc");

echo implode('=', $arr); // a=b=c
echo $arr1[1]; // bbb

```



****

``` php
// __construct, __destruct, __call,
// __callStatic, __get, __set, __inset, __unset, __sleep, __wakeup, __clone
class People {
    var $id; // public/protected/private
    var $name;
    
    private $n;
    
    // 
    function __construct($id, $name, $n) {
        $this->id = $id;
        $this->name = $name;
        $this->n = $n;
    }
    
    // 
    function __destruct() {
        
    }
    
    // clone
    function __clone() {
        echo "clone function"
    }
    
    // 
    function __call($name, $arguments) {
        echo 'call function';
    }
    // __callStatic
    // BTW People::FnSelf::Fn
    static function __callStatic($name, $arguments) {
        echo "$name".implode(',', $arguments);
    }
    
    //  $p->nprivate
    // __get
    function __get($property) {
        echo $this->$property;
    }
    
    //  $p->nprivate
    // __set
    function __set($property, $value) {
        $this->$property = $value;
    }
    
    
    function getInfo() {
        echo $this->id.''.$this->name;
    }
}

$p = new People(1, 'akara');
$p->getInfo();

$p->test(); // __call

echo $p->n; // __get
$p->n = 1; // __set

$s = clone $p; // __clone



$data = serialize($arr); // 
echo $data;
unserialize($data); // 

// serialize__sleep
// unserialize__wakeup
```



**PHP**

``` php
<?php
    $link = mysqli_connect('localhost:3308', 'root', '');
    mysqli_select_db($link, 'test');
    $sql = "SELECT * FROM comments";
    $result = mysqli_query($link, $sql);
    if (mysqli_num_rows($result) > 0) {
        while ($row = mysqli_fetch_assoc($result)) {

        }
    }

    mysqli_close($link)
?>
```
