---
sidebar_position: 1
title: 
---

## 



 `  ;` 

- <Highlight>, , </Highlight>

- <Highlight></Highlight>

- <Highlight>,</Highlight>

:::info
 C , 

- (`a~z` `A~Z`)
- 
- 
-  C 
- , test  Test 
:::


<HoverText text="" explanation="" />

```c showLineNumbers
// ,
int value;
//  = , 
//  = “” C  
//  =  (998=value,) 
value = 998; 

// 
int a = 4;

// (,)
int b = 4, c = 2;

// 
int  d = 3, e;
e = 5;

// 
int i = 10;
i = 20; // 

// 
int a = 10;
int b = a; // a10b
```

:::tip
,  =   
:::



## 

****`#define  `  `const   = ;`

- <Highlight></Highlight>
- <Highlight></Highlight>
- <Highlight></Highlight>

:::info
**#define  const **

- **#define**: 
- **const**: 

`const`  C 
:::
### #define

```c showLineNumbers
#include <stdio.h>
//  #define 
#define PI 3.14159

int main(){
    printf("PI = %f\n", PI);
    return 0;
}
```

### const

```c showLineNumbers
#include <stdio.h>

int main(){
    //  const 
    const int MAX_SIZE = 100;
    //  const 
    const int MIN_SIZE = 10, DEFAULT_SIZE = 50;
    //  const 
    const double AAA = 100.0, 
                 BBB = 200.0, 
                 CCC = 300.0;
    printf("MAX_SIZE = %d\n", MAX_SIZE);
    return 0;
}
```

### enum

,ABC1-7

“”, “”

* C ****
*  1 0
* 
* 

`#define`

<Highlight color="g">()</Highlight>

:

```c showLineNumbers
enum{
    1,
    2,
    ……
};
```

#### 

```c showLineNumbers
#include <stdio.h>
enum Demo {
    Spring,      //  0
    // Spring = 100, // 
    Summer = 10, //  10
    Autumn,      //  1 11
    Winter = 'A', // char 'A'ASCII 65
    A,            //  1 66
    //B = 1.0,      // 
}; 

int main(int argc, const char * argv[]) {
    //  s
    //  enum Demo  int int s; 
    // 
    //   enum Demo s;
    enum Demo s = Spring;
    printf("s = %d\n", s); // 0
    // 
    s = Summer;
    printf("s = %d\n", s); // 10
    s = Autumn;
    printf("s = %d\n", s); // 11
    s = Winter;
    printf("s = %d\n", s); // 65
    s = A;
    printf("s = %d\n", s); // 66
    // 
    // 
    s = 100; 
    printf("s = %d\n", s); // 100
    return 0;
}
```

:::tip


typedef

```c showLineNumbers
typedef enum {
    STATE_A,
    STATE_B
} StateType;

// stateStateTypeintdoublechar
StateType state = STATE_A;
```
:::


:::warning



```c showLineNumbers
enum Season {
    Spring,
    Summer,
    Autumn,
    Winter
} s;//

// enum Season s;

s = Spring; //  s = 0;
// 
s = 3; //  s = winter;
printf("%d", s);


// 
// 
enum {
    A,
    B,
} Num;
```
:::


## 

C ,

- <Highlight>**** `{}` </Highlight>
- `{}` <Highlight></Highlight> `{}` 

### 

- 
- **__**, , 

```c showLineNumbers
// 
int call(){
    int i = 998; 
    int i = 666; // , 
    return 0;
}

int main(){
    int a ;
    {// 
      int a = 998;
      int b = 666;
    }
    b = 100; // , b
    return 0;
}
```

### 

- ,

```c showLineNumbers
int i = 666;
int i = 998; // 
int main(){
    printf("i = %d\n", i); // 
    return 0;
}// 
int call(){
    printf("i = %d\n", i); // 
    return 0;
}
```

   shadow

```c showLineNumbers
#include <stdio.h>
int i = 666;
int main(){
    int i = 998; // 
    //shadow
    printf("i = %d\n", i); // 998
    return 0;
}
```



## putchar  getchar



```c showLineNumbers
#include <stdio.h>
int main(){
    char ch = 'a';
    putchar(ch); // a
}
```



```c showLineNumbers
#include <stdio.h>
int main(){
    char ch;
    ch = getchar();// 
    printf("ch = %c\n", ch);
}
```

## printf

printf , f  format,

printf 

1. 

 printf ********

```c showLineNumbers
#include <stdio.h>
int main(){
    // 
    printf("Hello, World!\n");
    printf("AaBb\n");
    printf("\n");
    return 0;
}
```

2. 


:`printf("",);`

: `%[][][.][]`

:
```c showLineNumbers
#include <stdio.h>
int main(){
    int a = 10;
    int b = 20;
    // : 10 20
    printf("%d %d",a, b);

    // 
    printf("%d %d %d",a, b); // 
    return 0;
}
```

### 

, 

|   |                                 |  |
| ----- | ----------------------------------- | ---- |
| **** |
| c     |                             | `%c` |
| s     | char                  | `%s` |
| **** |
| d / i |  10                   | `%d`, `%i` |
| hd / hi | short int               | `%hd`, `%hi` |
| ld / li | long int                | `%ld`, `%li` |
| lld / lli | **long long int**   | `%lld`, `%lli` |
| u     |  10                   | `%u` |
| hu    | unsigned short int                 | `%hu` |
| lu    | unsigned long int                  | `%lu` |
| llu   | **unsigned long long int**         | `%llu` |
| **** |
| o     |  8                    | `%o` |
| lo    | unsigned long int8         | `%lo` |
| llo   | **unsigned long long int8** | `%llo` |
| x / X |  16                   | `%x`, `%X` |
| lx / lX | unsigned long int16       | `%lx`, `%lX` |
| llx / llX | **unsigned long long int16** | `%llx`, `%llX` |
| **** |
| f     | float/double 6    | `%f` |
| lf    | double                  | `%lf` |
| Lf    | long double        | `%Lf` |
| e / E |                 | `%e`, `%E` |
| Le / LE | long double           | `%Le`, `%LE` |
| g / G |           | `%g`, `%G` |
| **** |
| p     |                             | `%p` |
| n     |         | `%n` |
| %     |  %                    | `%%` |


```c showLineNumbers
#include <stdio.h>
int main(){
    // 
    char ch = 'A';
    char str[] = "Hello";
    printf(": %c\n", ch);           // A
    printf(": %s\n", str);        // Hello
    
    // 
    short short_num = 12345;
    int int_num = 123456;
    long long_num = 1234567890L;
    long long longlong_num = 1234567890123456789LL;
    
    printf("short: %hd\n", short_num);           // 12345
    printf("int: %d\n", int_num);               // 123456
    printf("long: %ld\n", long_num);            // 1234567890
    printf("long long: %lld\n", longlong_num);  // 1234567890123456789
    
    // 
    unsigned int uint_num = 4294967295U;
    unsigned long long ulonglong_num = 18446744073709551615ULL;
    
    printf("unsigned int: %u\n", uint_num);           // 4294967295
    printf("unsigned long long: %llu\n", ulonglong_num); // 18446744073709551615
    
    // 
    int num = 255;
    printf(": %d\n", num);        // 255
    printf(": %o\n", num);        // 377
    printf(": %x\n", num);      // ff
    printf("(): %X\n", num); // FF
    
    // long long 
    long long big_num = 255LL;
    printf("long long : %llo\n", big_num);    // 377
    printf("long long : %llx\n", big_num);   // ff
    
    // 
    float f_num = 3.14159f;
    double d_num = 3.141592653589793;
    
    printf("float: %f\n", f_num);       // 3.141590
    printf("double: %lf\n", d_num);     // 3.141593
    printf(": %e\n", d_num);   // 3.141593e+00
    printf(": %g\n", d_num);     // 3.14159
    
    // 
    int *ptr = &int_num;
    printf(": %p\n", ptr);       //  0x7fff5fbff6ac
    
    return 0;
}
```


### 

: `printf("a = %[]", a);`

, ,, 

```c showLineNumbers
#include <stdio.h>
int main(){
    // 
    int a = 1;
    printf("a =|%d|\n", a); // |1|
    printf("a =|%5d|\n", a); // |    1|
    // 
    int b = 1234567;
    printf("b =|%d|\n", b); // |1234567|
    printf("b =|%5d|\n", b); // |1234567|
}
```


### 

: `printf("a = %[][]", a);`

|  |                                                   |
| ---- | ----------------------------------------------------- |
| -    | ,                                     |
| +    | ,+,   |
| 0    | ,  0 .()              |
|  | ,,  |
| #    |  csdu                               |
| #    |  o ,  o                           |
| #    |  x , 0x                           |

```c showLineNumbers
#include <stdio.h>
int main(){
    int a = 1;
    int b = -1;
    // -
    printf("a =|%d|\n", a); // |1|
    printf("a =|%5d|\n", a); // |    1|
    printf("a =|%-5d|\n", a);// |1    |
    // +
    printf("a =|%d|\n", a); // |1|
    printf("a =|%+d|\n", a);// |+1|
    printf("b =|%d|\n", b); // |-1|
    printf("b =|%+d|\n", b);// |-1|
    // 0
    printf("a =|%5d|\n", a); // |    1|
    printf("a =|%05d|\n", a); // |00001|
    // 
    printf("a =|% d|\n", a); // | 1|
    printf("b =|% d|\n", b); // |-1|
    // #
    int c = 10;
    printf("c = %o\n", c); // 12
    printf("c = %#o\n", c); // 012
    printf("c = %x\n", c); // a
    printf("c = %#x\n", c); // 0xa
}
```


### 
: `printf("a = %[]", a); `

".", , , , 

```c showLineNumbers
#include <stdio.h>
int main(){
    double a = 3.1415926;
    printf("a = %.2f\n", a); // 3.14
}
```

- 
  - : `printf("a = %.*f", a);`

```c showLineNumbers
#include <stdio.h>
int main(){
    double a = 3.1415926;
    printf("a = %.*f", 2, a); // 3.14
}
```

- ()
  - ,%f , 6~7 
  - ,%lf , 15~16 
  - (), ,
  - 

```c showLineNumbers
#include <stdio.h>
int main(){
    //        1234.567871093750000
    float a = 1234.567890123456789;
    //         1234.567890123456900
    double b = 1234.567890123456789;
    printf("a = %.15f\n", a); // 8, 
    printf("b = %.15f\n", b); // 16, 
}
```


### 

: `printf("a = %[]", a);`

|  |       |                |
| ---- | ------------- | ------------------ |
| hh   | dioux |  char          |
| h    | dioux |  short int     |
| l    | dioux |  long int      |
| ll   | dioux |  long long int |

```c showLineNumbers
#include <stdio.h>
int main(){
    char a = 'a';
    short int b = 123;
    int  c = 123;
    long int d = 123;
    long long int e = 123;
    printf("a = %hhd\n", a); // 97
    printf("b = %hd\n", b); // 123
    printf("c = %d\n", c); // 123
    printf("d = %ld\n", d); // 123
    printf("e = %lld\n", e); // 123
}
```

### 

: `printf("%f%%", 3.1415);`

%, %

```c showLineNumbers
#include <stdio.h>
int main(){
    printf("%f%%", 3.1415); // 3.1415%
}
```


## scanf

scanf , , scanf `\n`scanf

:`scanf("", );`

, &+

```c showLineNumbers
#include <stdio.h>
int main(){
    int number;
    scanf("%d", &number); // 
    printf("number = %d\n", number);
}
```

### 



```c showLineNumbers
#include <stdio.h>
int main(){
    int number;
    int value;
    // 
    //    
    scanf("%d %d", &number, &value);
    printf("number = %d\n", number);
    printf("value = %d\n", value);
}
```

<Highlight></Highlight>

```c showLineNumbers
#include <stdio.h>
int main(){
    int number;
    int value;
    //  , 
    scanf("%d,%d", &number, &value);
    printf("number = %d\n", number);
    printf("value = %d\n", value);
}
```

`number = `

```c showLineNumbers
#include <stdio.h>
int main(){
    int number;
    // number =   , 
    // 10number = 10
    scanf("number = %d", &number);
    printf("number = %d\n", number);
}
```

### 

, Tab 

```c showLineNumbers
#include <stdio.h>
int main(){
    float num;
    // : Tab    Tab  3.14 , 3.14
    scanf("%f", &num);
    printf("num = %f\n", num);
}
```

### 

- 
- scanf 
- ,scanf ,

```c showLineNumbers
#include <stdio.h>
int main(){
    int num1;
    int num2;
    char ch1;
    // 10a20b30
    // 
    scanf("%d%c%d", &num1, &ch1, &num2);
    printf("num1 = %d, ch1 = %c, num2 = %d\n", num1, ch1, num2);
    char ch2;
    int num3;
    // 
    scanf("%c%d",&ch2, &num3);
    printf("ch2 = %c, num3 = %d\n", ch2, num3);
    //
}
```





```c showLineNumbers
#include <stdio.h>
int main(){
    int num1;
    int num2;
    char ch1;
    scanf("%d%c%d", &num1, &ch1, &num2);
    printf("num1 = %d, ch1 = %c, num2 = %d\n", num1, ch1, num2);
    
    // getcharEOF
    int c;
    while ((c = getchar()) != '\n' && c != EOF);

    // 
    setbuf(stdin, NULL); 
    
    char ch2;
    int num3;
    scanf("%c%d",&ch2, &num3);
    printf("ch2 = %c, num3 = %d\n", ch2, num3);
}
```
