
:::tip 

Babel[](https://messiahhh.github.io/blog/docs/%E7%BC%96%E8%AF%91%E5%8E%9F%E7%90%86)

:::

`@babel/core``babel`**`JS`**`@babel/cli``babel``webpack``@babel/core``babel-loader`

****

``` bash
npm install --save-dev @babel/core @babel/cli
```

``` bash
npx babel src --out-dir output # srcoutput
npx babel src --out-dir output --extensions ".ts" # @babel/preset-typescript
npx babel src --out-dir output --plugins=@babel/plugin-transform-arrow-functions
```



## plugin

`babel`

``` bash
npm install --save-dev @babel/plugin-transform-arrow-functions @babel/plugin-proposal-class-properties
```

``` bash
npx babel src --out-dir output --plugins=@babel/plugin-transform-arrow-functions
```

## preset



### @babel/preset-env

`JS`

``` js
// a.js 
export default function() {
    console.log('hello');
}

export function A() {
    console.log('aka');
}

// a.js 
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;
exports.A = A;

function _default() {
  console.log('hello');
}

function A() {
  console.log('aka');
}
```

``` js
// b.js 
import fn from './a'
fn()

// b.js 
"use strict";

var _a = _interopRequireDefault(require("./a"));

function _interopRequireDefault(obj) { 
    return obj && obj.__esModule ? obj : { default: obj }
}

(0, _a.default)();
```

### @babel/preset-react

`JS`

### @babel/preset-typescript

`TS`



## browsersList

`@babel/preset-env``polyfill`

`babel`

``` js
module.exports = {
    "presets": [
        [
            "@babel/preset-env", {
                "targets": {
                    "edge": "17",
                    "firefox": "60",
                    "chrome": "67",
                    "safari": "11.1"
                }
            }
        ]
    ]
}
```

`browsersList``package.json``.browserslistrc.js`

``` json
// package.json
{
    "browserslist": [
        "defaults",
        "not IE 11",
        "maintained node versions"
  	]
}
```





## 

### `@babel/register`

Node

``` bash
npm install @babel/core @babel/register -S
```

``` js
// index.js
require('@babel')
require('./app.js')

// app.js
import koa from 'koa'
```

``` bash
node index.js
```



### `@babel/polyfill`

`@babel/polyfill``core-js/stable``regenerator-runtime/runtime`[](https://babeljs.io/docs/en/babel-polyfill)

``` js
import "core-js/stable";
import "regenerator-runtime/runtime";
```






