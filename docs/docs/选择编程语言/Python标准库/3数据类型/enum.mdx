
## enum 



```python showLineNumbers
from enum import Enum

class TrafficLight(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
    # GREEN = 4 # 

    # 
    A = None # NoneFalseTrue
    B = [255, 0, 0] # 
    C = 1 # 

# 
light = TrafficLight.RED

# if light == 1   Enum
if light == TrafficLight.RED:
    print("")
elif light == TrafficLight.YELLOW:
    print("")
else:
    print("")
```

`Enum``Enum`

#### 1. ****
   `Color.RED``1``'red'``Enum`
   
   
   ```python
   status = Status.SUCCESS  # 
   ```
   
   ```python
   status = 1  #  1 
   ```

#### 2. ****
   `Enum``Enum`
   
   `Status.SUCCESS = 1``Status.SUCCESS = 2` bug

#### 3. ****
   `Enum` bug
   
   
   ```python
   def set_color(color: Color):
       if not isinstance(color, Color):
           raise ValueError("Invalid color")
   ```

#### 4. ****
   `Enum`
   
   
   ```python
   class TrafficLight(Enum):
       RED = 1
       YELLOW = 2
       GREEN = 3
       FLASHING = 4  # 
   ```

#### 5. ****
   `Enum`

#### 6. ****
   `Enum``Enum`
   
   ```python
   for state in TrafficLight:
       print(state)
   ```


