---
sidebar_position: 14
---
# 



## `<video>`

`<video>``<video>`

```html
<video src="example.mp4" controls>
  <p> HTML5 <a href="example.mp4"></a></p>
</video>
```

`<video>`

`<video>`

- `src`
- `controls`
- `width`
- `height`
- `autoplay`
- `loop`
- `muted`
- `poster` URL
- `preload``autoplay``none``metadata``auto`
- `playsinline`iPhone  Safari 
- `crossorigin``anonymous` Cookie`use-credentials`
- `currentTime`
- `duration``+Infinity`



```html
<video width="400" height="400"
       autoplay loop muted
       poster="poster.png">
</video>
```

 400 x 400

HTML `<source>`

```html
<video controls>
  <source src="example.mp4" type="video/mp4">
  <source src="example.webm" type="video/webm">
  <p> HTML5 <a href="example.mp4"></a></p>
</video>
```

`<source>``type` MIME MP4  WebM MP4 MP4  MP4 WebM

## `<audio>`

`<audio>``<video>`

```html
<audio controls>
  <source src="foo.mp3" type="audio/mp3">
  <source src="foo.ogg" type="audio/ogg">
  <p> HTML5 <a href="foo.mp3"></a></p>
</audio>
```

`<audio>``<source>` MP3  Ogg 

`<audio>``<video>`

- `autoplay`
- `controls`
- `crossorigin`
- `loop`
- `muted`
- `preload`
- `src`

## `<track>`

`<track>` WebVTT `.vtt``<video>`

```html
<video controls src="sample.mp4">
   <track label="" kind="subtitles" src="subtitles_en.vtt" srclang="en">
   <track label="" kind="subtitles" src="subtitles_cn.vtt" srclang="cn" default>
</video>
```



`<track>`

- `label`
- `kind``subtitles``captions`
- `src`vtt 
- `srclang`
- `default`

## `<source>`

`<source>``<picture>``<video>``<audio>`



- `type` MIME 
- `src``<video>``<audio>`
- `srcset``<picture>`
- `media``<picture>`
- `sizes``<picture>``srcset`

## `<embed>`

`<embed>`



```html
<embed type="video/webm"
       src="/media/examples/flower.mp4"
       width="250"
       height="200">
```

 MP4 

`<embed>`

- `height`
- `width`
- `src` URL
- `type` MIME 

`type` MIME 

 QuickTime  MOV 

```html
<embed type="video/quicktime" src="movie.mov" width="640" height="480">
```

 Flash 

```html
<embed src="whoosh.swf" quality="medium"
       bgcolor="#ffffff" width="550" height="400"
       name="whoosh" align="middle" allowScriptAccess="sameDomain"
       allowFullScreen="false" type="application/x-shockwave-flash"
       pluginspage="http://www.macromedia.com/go/getflashplayer">
```

 Flash `pluginspage`

## `<object>``<param>`

`<object>``<embed>``<embed>`

 PDF 

```html
<object type="application/pdf"
    data="/media/examples/In-CC0.pdf"
    width="250"
    height="200">
</object>
```

 PDF  PDF 

`<object>`

- `data` URL
- `form``id`
- `height`
- `width`
- `type` MIME 
- `typemustmatch``data``type`

 Flash 

```html
<object data="movie.swf"
  type="application/x-shockwave-flash"></object>
```

`<object>``<param>`

```html
<object data="movie.swf" type="application/x-shockwave-flash">
  <param name="foo" value="bar">
</object>
```

