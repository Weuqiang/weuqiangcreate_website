---
sidebar_position: 6
title: glob
description: "glob 按 Unix shell 风格的模式匹配文件路径，支持通配符与递归搜索"
---

## 学习目标

- 用 `glob` / `iglob` 按通配符匹配文件路径
- 掌握 `*` `?` `[]` `**` 的含义与递归
- 知道结果无序、需自行 `sorted`

## 前置知识

- 文件系统与路径基本概念
- Python 字符串与列表

## 核心概念

`glob` 用 Unix shell 风格通配符找文件，比手动 `listdir`+过滤简洁。`*` 不含分隔符，`**`+`recursive=True` 递归。返回顺序不确定；大目录用 `iglob` 惰性迭代省内存。它只匹配文件名，不关心文件内容。

# glob — 文件路径模式匹配

`glob` 模块用 **Unix shell 风格的通配符**找出所有匹配特定模式的文件路径，比手动 `os.listdir` + 过滤更简洁。注意：返回结果的**顺序是不确定的**，需要有序请自行 `sorted()`。

## 匹配规则

`glob` 的语法和 shell 类似：

- `*` 匹配任意数量字符（不含路径分隔符 `/`）。
- `?` 匹配单个字符。
- `[abc]` 匹配 `a`、`b` 或 `c`；`[!abc]` 匹配除此之外的任意字符。
- `**`（配合 `recursive=True`）递归匹配任意层级的目录。

## `glob.glob`：返回列表

```python showLineNumbers
import glob

# 当前目录下所有 .py 文件
print(glob.glob("*.py"))

# 某目录下所有 .md 文件
print(glob.glob("docs/**/*.md", recursive=True))
```

`recursive=True` 时，`**` 会深入子目录递归匹配；不加则 `**` 只等价于 `*`。

## `glob.iglob`：惰性迭代器

文件很多时，`iglob` 逐个产出匹配项，避免一次性把所有路径装进内存：

```python showLineNumbers
import glob

for path in glob.iglob("**/*.log", recursive=True):
    print(path)
```

## 递归与目录：`**`

`**` 是处理"整棵目录树"的利器：

```python showLineNumbers
import glob

# 找出项目里所有图片
images = glob.glob("assets/**/*.png", recursive=True)
```

## `glob.escape`：转义特殊字符

当路径里本身含有 `*`、`?`、`[` 等字符（比如文件名就叫 `report[1].txt`）时，用 `escape` 把它们当普通字符处理，防止被当成通配符：

```python showNumbers
import glob

pattern = glob.escape("report[1].txt")   # 变成字面量匹配
print(glob.glob(pattern))
```

## `glob.translate`：转成正则

`translate` 把通配符模式编译成正则表达式对象，方便你复用或做更精细的控制：

```python showLineNumbers
import glob, re

rx = glob.translate("*.py")
print(rx.pattern)            # 类似 '(?s:.*\\.py)\\Z'
print(bool(rx.match("a.py")))
```

## 示例

```python
import glob

# 当前目录所有 .py
print(glob.glob("*.py"))

# 递归查找所有 .md（含子目录）
print(glob.glob("docs/**/*.md", recursive=True))

# 迭代器，省内存
for path in glob.iglob("**/*.png", recursive=True):
    print(path)
```

## 易错点

- 结果顺序不确定：不要假设 `glob` 返回有序，需要排序自己 `sorted()`。
- `**` 必须 `recursive=True`：否则 `**` 只等价于 `*`，不会深入子目录。
- `*` 不含路径分隔符：想匹配跨目录要用 `**`。
- 只匹配文件名不读内容：按内容筛选要自己打开文件判断。

## 练习

- 列出某目录下所有大于 1MB 的图片。
- 用 `glob` 收集所有 `.py` 并统计总行数。
- 递归查找名字含「test」的文件。

## 小结

- 简单匹配用 `glob()`；大目录用 `iglob()` 省内存。
- 跨层级搜索记得 `recursive=True` + `**`。
- 文件名含通配符字样时先 `escape`。

> 延伸：需要更底层的目录遍历（含文件属性、递归控制），可用标准库的 `os.walk`；本目录的 [`tempfile`](../tempfile) 则用于安全地创建临时文件。

## 延伸阅读

- [tempfile 临时文件与目录](../tempfile)
