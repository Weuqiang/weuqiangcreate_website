---
sidebar_position: 2
title: 
---

C5`char``int``float``double``void`

2
- `short``int`
- `long` 1~2 `int` 1 `double`

2()
- `signed` `signed`
- `unsigned`

14

|  |  |  |  |  |  |  |  |
|--------:|--------:|--------:|------|----------|----------|----------|------|
|  |  | 1 | `char` | `signed char` | `char` | -2⁷ ~ 2⁷-1 | 8****|
|  |  | 1 | `unsigned char` | `unsigned char` | `unsigned char` | 0 ~ 2⁸-1 | 8**** |
|  |  | 2 | `short` | `signed short int` | `short`/`short int` | -2¹⁵ ~ 2¹⁵-1 | 16 |
|  |  | 2 | `unsigned short` | `unsigned short int` | `unsigned short` | 0 ~ 2¹⁶-1 | 16 |
|  |  | 4 | `int` | `signed int` | `int`/`signed` | -2³¹ ~ 2³¹-1 | 32 |
|  |  | 4 | `unsigned int` | `unsigned int` | `unsigned int`/`unsigned` | 0 ~ 2³²-1 | 32 |
|  |  | 4/8 | `long` | `signed long int` | `long` | -2³¹ ~ 2³¹-1  -2⁶³ ~ 2⁶³-1 | 3264 |
|  |  | 4/8 | `unsigned long` | `unsigned long int` | `unsigned long` | 0 ~ 2³²-1  0 ~ 2⁶⁴-1 | 3264 |
|  |  | 8 | `long long` | `signed long long int` | `long long int`/`signed long long`/`long long` | -2⁶³ ~ 2⁶³-1 | 64 |
|  |  | 8 | `unsigned long long` | `unsigned long long int` | `unsigned long long` | 0 ~ 2⁶⁴-1 | 64 |
|  |  | 4 | `float` | `float` | `float` | ±1.18×2⁻¹²⁶ ~ ±3.40×2¹²⁷ | 32IEEE754 |
|  |  | 8 | `double` | `double` | `double` | ±2.23×2⁻¹⁰²² ~ ±1.80×2¹⁰²³ | 64IEEE754 |
|  |  | 8/12/16 | `long double` | `long double` | `long double` |  | 64/80/128 |
|  | - | 0 | `void` | `void` | `void` | - |  |


## char

char  ASCII 

ASCII 

- <Highlight></Highlight><Highlight></Highlight>ASCII  `'A'`, `'1'`, `'$'`, `'\n'`

<Highlight>C</Highlight><HoverText text="" explanation="" />


```c showLineNumbers
#include <stdio.h>
int main(){
    const char letter = 'A';
    const char newline = '\n';
    const char tab = '\t';
    
    printf(": %c\n", letter);
    printf("%c\n", newline);
    printf("%c\n", tab);
    return 0;
}
```

`char``int``int``char``ASCII``char``int``ASCII`

ASCII [https://www.asciitable.com/](https://www.asciitable.com/)

:::tip


-  `"Hello"`, `"C"`
- <Highlight> '\0' </Highlight>

```c showLineNumbers
#include <stdio.h>
#include <string.h>
int main(){
    const char greeting[] = "Hello World";
    const char empty[] = "";
    
    printf(": %s\n", greeting);
    printf(": %lu\n", strlen(empty));
    return 0;
}
```
:::


## int



-  `666`, `-120`, `0`
-  `0b`  `0b1010`10
-  `0`  `0123`83
-  `0x`  `0x123`291

```c showLineNumbers
#include <stdio.h>
int main(){
    printf(": %d\n", 123);      // 123
    printf(": %d\n", 0123);     // 83
    printf(": %d\n", 0x123);  // 291
    printf(": %d\n", 0b1010);   // 10
    return 0;
}
```



```c showLineNumbers
+178

0b325 // 0-1
0b0010

0986 // 0-7
00011

0x001
0x7h4 // 0-9,a-f,A-F
0xffdc
```

## signed  unsigned



- `signed` - 
- `unsigned` - 

`signed`  `unsigned`  `short`  `long` 


:::tip
`float``double``long double`

`signed`  `int` 

 `signed`  `int`  `signed int` 

:::


## short  long

- `short``int`
- `long` 1~2 `int` 1 `double`


:::tip

  `long`  `int` 48

**C**

```c showLineNumbers
// C
sizeof(char) <= sizeof(short) <= sizeof(int) <= sizeof(long) <= sizeof(long long)
//    1      ≥2         ≥2        ≥4         ≥8
```

**1980-199032**
```c showLineNumbers
// ""
char  = 1  (8)
short = 2  (16) 
int   = 4  (32) ← CPU
long  = 4  (32) ← 32
```

**200064**
```c
// 
// Linux/macOS (LP64)
int   = 4  (32) ← 
long  = 8  (64) ← 64

// Windows (LLP64)  
int   = 4  (32) ← 
long  = 4  (32) ← 32
long long = 8  (64) ← long long64
```

****
```c
//  
#include <stdint.h>
int32_t  x;    // 32
int64_t  y;    // 64
uint32_t z;    // 32

//  long
long maybe_big;    // 
```
:::


## floatdouble

****

- <HoverText text="" explanation="46-7" /> `f`  `F`  `3.14f`
- <HoverText text="" explanation="815-16" /> `3.14159`
-  `e`  `E`  `1.23e5`123000

```c showLineNumbers
#include <stdio.h>
int main(){
    const float pi_f = 3.14f;          // 
    const double pi_d = 3.14159;       // 
    const double big_num = 1.23e5;     // 
    const double small_num = 1.23e-3;  // 0.00123
    
    printf("PI: %.7f\n", pi_f);
    printf("PI: %.15f\n", pi_d);
    printf(": %.0f\n", big_num);
    printf(": %.5f\n", small_num);
    return 0;
}
```



```c showLineNumbers
10.98    //  
.089     //  00.089
-.003    //  
96.0f    //  
3.14E-2  //  0.0314

96f      //  
96.oF    //  'o''0'96.0F
3.14.15  //  
```

:::info
`long double` 

`long int``long double`""

C`long double`

**1980**

Intelx878087801064double

**LinuxGCC** Intel80

**Microsoft** Visual Studio`long double`64`double`
- 
- Windows
- 64

**2000**

**ARM** ARM
- 80
- 
- 64

**Google** ARM64

****



**IBM AIX/PowerPC** """6464" Double-Double31

**GCC** `__float128`"128IEEE" 34

****

| / | long double |  |
|------------|----------------|----------|
| **Windows + Visual Studio** | 864 |  |
| **Linux + GCC (x86)** | 12/1680 | x87 |
| **Mac + Clang (Intel)** | 1680 | x86-64 ABI |
| **Mac + Clang (Apple Silicon)** | 864 | ARM |
| **Android** | 864 |  |
| **IBM AIX/PowerPC** | 16128 |  |
| **GCC __float128** | 16128 | IEEE 754 |

3232464648

80103233x4=126422x8=16


****
- `long double``double`
- 
- 

:::


## void

**`void` **

|  |  |  |  |
|------|------|------|------|
| **** | `void ()` | `void print_hello() { printf("Hello"); }` |  |
| **** | `(void)` | `int get_random(void) { return 42; }` |  |
| **** | `void*` | `void *ptr; ptr = &some_variable;` |  |

```c
//  void 
void print_message(void) {          // 
    printf("Hello World\n");
}

int main(void) {                    // int
    void *generic_ptr;              // 
    int num = 10;
    generic_ptr = &num;             // int
    
    print_message();                // void
    return 0;
}

//  void 
void variable;                      // void
void array[10];                     // void
```

****
- `void` 
- `void*` 
-  `void` C89


## 



### 

`()()`

```c showLineNumbers
// doubleint
int a = (int)(10.5 + 0.6);
// C 0(0)
// 11

// 
int a = (int) 10.5;
// 10

// 
int a = (int) 10.5 + 0.6;
// 10
// (int) 10.5  10int
// 10int+ 0.6double→ 10int10.0double10.6double
// 10.6doubleinta10
// C 0
```

### 

```c showLineNumbers
// ,
int a = 10.6;
// 10
// C 0(0)
```

### 

“”

, 

```c showLineNumbers
// 0, 0double
double a = (double)(1 / 2);

// 0.5, 1double, 2double
double b = (double)1 / 2;
// 
double b = 1.0 / 2;
```

- 

```c showLineNumbers
#include <stdio.h>
int main(){
    double d = 3.14;
    int num = (int)d;
    printf("num = %i\n", num); // 3
    printf("d = %lf\n", d); // 3.140000
}
```
