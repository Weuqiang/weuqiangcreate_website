---
sidebarDepth: 4
---
# Linux

LinuxLinux

## 



```bash
Ctrl + A        # ←
Ctrl + E        # 
Ctrl + U        # 
Ctrl + K        # 
Ctrl + W        # 
Ctrl + L        # clear
Ctrl + C        # 
Ctrl + Z        # fg
Ctrl + R        # 
Ctrl + D        # exit
```

## 

### 

```bash
# 
ls                  # 
ls -l               # 
ls -a               # .
ls -lh              # 1K2M
ls -lt              # 

# 
cd /path/to/dir     # 
cd ~                # /home/
cd -                # 
cd ..               # 

# 
pwd

# 
mkdir dir           # 
mkdir -p a/b/c      # 

# 
rm file             # 
rm -r dir           # 
rm -rf dir          # 
rmdir dir           # 

# 
cp file1 file2      # 
cp -r dir1 dir2     # 

# /
mv file1 file2      # 
mv file dir/        # 

# 
touch file          # 
```

### 

```bash
# 
cat file            # 
head file           # 10
head -n 20 file     # 20
tail file           # 10
tail -n 20 file     # 20
tail -f file        # 

# 
less file           # q
more file           # less

# 
file filename       # 
```

### 

```bash
# 
ls -l file
# -rwxr-xr-x  1 user group 1234 Jan 1 12:00 file
#    
#     r-x = +
#    r-x = +
#   rwx = ++
#  - = d = 

# 
chmod 755 file      # rwxr-xr-x
chmod 644 file      # rw-r--r--
chmod +x file       # 
chmod -x file       # 

# 
chown user file
chown user:group file
chown -R user:group dir  # 

# 
# r()=4, w()=2, x()=1
# 755 = 7(4+2+1) 5(4+1) 5(4+1) = rwxr-xr-x
# 644 = 6(4+2) 4(4) 4(4) = rw-r--r--
# 777 = 
```

## 

### grep - 

```bash
# 
grep "pattern" file
grep "error" app.log        # error

# 
grep -i "pattern" file      # 
grep -r "pattern" dir/      # 
grep -n "pattern" file      # 
grep -v "pattern" file      # 
grep -c "pattern" file      # 
grep -l "pattern" *.txt     # 
grep -A 3 "pattern" file    # 3
grep -B 3 "pattern" file    # 3
grep -C 3 "pattern" file    # 3

# 
grep -E "error|warning" file    # errorwarning
grep "^start" file              # start
grep "end$" file                # end

# 
grep -i "exception" *.log       # 
grep -rn "TODO" src/            # TODO
```

### sed - 

```bash
# 
sed 's/old/new/' file           # 
sed 's/old/new/g' file          # 
sed 's/old/new/gi' file         # 
sed -i 's/old/new/g' file       # 

# 
sed '/pattern/d' file           # 
sed '1d' file                   # 
sed '1,5d' file                 # 15

# 
sed -i 's/localhost/0.0.0.0/g' config.ini    # 
sed -i '/^#/d' file                          # 
```
sed '1d' file                   # 
sed '1,5d' file                 # 1-5

# 
sed '1i\new line' file          # 
sed '1a\new line' file          # 
```

### awk - 

```bash
# 
awk '{print $1}' file           # 
awk '{print $1, $3}' file       # 13
awk '{print $NF}' file          # 

# 
awk '$3 > 100' file             # 3100
awk '/pattern/ {print $1}' file # 1

# 
awk '{sum+=$1} END {print sum}' file    # 
awk '{count++} END {print count}' file  # 
```

### 

```bash
# 
sort file               # 
sort -r file            # 
sort -n file            # 
sort -u file            # 

# 
uniq file               # 
sort file | uniq        # 
uniq -c file            # 

# 
wc file                 #   
wc -l file              # 
wc -w file              # 

# 
diff file1 file2        # 
```

## 

```bash
# 
ps                      # 
ps aux                  # 
ps aux | grep nginx     # 

# 
top                     # 
htop                    #  top

# 
kill PID                # 
kill -9 PID             # 
killall nginx           #  nginx 
pkill -f "python app"   # 

# 
command &               # 
nohup command &         # 
nohup command > output.log 2>&1 &  # 

# 
jobs                    # 
fg %1                   # 1
bg %1                   # 1
```

## 

```bash
# 
uname -a                # 
hostname                # 
uptime                  # 
date                    # 

# CPU 
free -h                 # 
df -h                   # 
du -sh dir              # 
du -h --max-depth=1     # 

# 
ifconfig                # 
ip addr                 # 
netstat -tuln           # 
ss -tuln                # 
ping host               # 
curl ifconfig.me        #  IP
```

## 

```bash
# 
wget url                # 
wget -O file url        # 
curl -O url             # 
curl url > file         # 

# HTTP 
curl url                # GET 
curl -X POST url        # POST 
curl -H "Content-Type: application/json" -d '{"key":"value"}' url

# SSH
ssh user@host           # 
ssh -p 2222 user@host   # 
ssh-keygen              #  SSH 
ssh-copy-id user@host   # 

# SCP - 
scp file user@host:/path        # 
scp user@host:/path/file .      # 
scp -r dir user@host:/path      # 
```

## 

```bash
# tar.gz
tar -czf archive.tar.gz dir/    # 
tar -xzf archive.tar.gz         # 
tar -xzf archive.tar.gz -C dir/ # 

# tar.bz2
tar -cjf archive.tar.bz2 dir/   # 
tar -xjf archive.tar.bz2        # 

# zip
zip -r archive.zip dir/         # 
unzip archive.zip               # 
unzip archive.zip -d dir/       # 

# 
tar -tzf archive.tar.gz         #  tar.gz
unzip -l archive.zip            #  zip
```

## 

```bash
# 
find /path -name "*.txt"        # 
find /path -type f              # 
find /path -type d              # 
find /path -size +100M          # 100M
find /path -mtime -7            # 7
find /path -name "*.log" -delete # 

# 
which command           # 
whereis command         # 
type command            # 
```

## 

```bash
# 
whoami                  # 
who                     # 
id                      #  ID  ID
groups                  # 

# 
useradd username        # 
useradd -m -s /bin/bash username  #  shell
passwd username         # 

# 
userdel username        # 
userdel -r username     # 

# 
su username             # 
su -                    #  root
sudo command            #  root 
```

## 

```bash
# 
env                     # 
echo $PATH              #  PATH
echo $HOME              #  HOME

# 
export VAR=value        # 
export PATH=$PATH:/new/path  #  PATH

# 
# ~/.bashrc  ~/.zshrc
echo 'export VAR=value' >> ~/.bashrc
source ~/.bashrc        # 
```

## 

```bash
# crontab
crontab -l              # 
crontab -e              # 
crontab -r              # 

# crontab 
#      
# * * * * * command
# 0 2 * * * /path/to/backup.sh  # 2

# 
# */5 * * * * command           # 5
# 0 */2 * * * command           # 2
# 0 0 * * 0 command             # 
# 0 0 1 * * command             # 1
```

## 

### 

```bash
# 
command1 | command2     # command1  command2 
ps aux | grep nginx     # 
cat file | wc -l        # 

# 
command > file          # 
command >> file         # 
command 2> error.log    # 
command > output.log 2>&1  # 
command < input.txt     # 
```

### 

```bash
# 
command1; command2      # 

# 
command1 && command2    # command1  command2
command1 || command2    # command1  command2

# 
command &               # 
```

### 

```bash
# 
alias ll='ls -lh'
alias gs='git status'
alias ..='cd ..'

#  ~/.bashrc
echo "alias ll='ls -lh'" >> ~/.bashrc
source ~/.bashrc
```

### 

```bash
history                 # 
!100                    # 100
!!                      # 
!$                      # 
```

## VIM 

```bash
# 
vim file

# 
i                       # 
Esc                     # 
:                       # 

# 
h j k l                 # 
w                       # 
b                       # 
0                       # 
$                       # 
gg                      # 
G                       # 

# 
x                       # 
dd                      # 
yy                      # 
p                       # 
u                       # 
Ctrl+r                  # 

# 
/pattern                # 
?pattern                # 
n                       # 
N                       # 

# 
:w                      # 
:q                      # 
:wq                     # 
:q!                     # 
```

## 

```bash
# 
/etc/profile            # 
/etc/hosts              # 
/etc/hostname           # 
/etc/resolv.conf        # DNS 

# 
~/.bashrc               # Bash 
~/.zshrc                # Zsh 
~/.vimrc                # Vim 
~/.ssh/config           # SSH 
~/.ssh/authorized_keys  # SSH 

# 
/etc/nginx/nginx.conf   # Nginx 
/etc/mysql/my.cnf       # MySQL 
```

## 

```bash
# CPU
top                     # 
mpstat                  # CPU 

# 
free -h                 # 
vmstat                  # 

#  I/O
iostat                  # I/O 
iotop                   # I/O 

# 
iftop                   # 
nethogs                 # 
```

## 

1. ** Tab **
2. ****Ctrl+R 
3. ****
4. ****
5. **** root
6. ****
7. ** vim**
