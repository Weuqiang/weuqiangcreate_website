---
sidebar_position: 6
---
# 

## 

attribute HTML “”

```html
<html lang="en">
```

`<html>``lang="en"``html``lang``en`

`lang``LANG`

`=`

“”“”

```html
<input type="text" required>
```

`required``<input>`

## 

global attributes



### id

`id``<p>``id``<p>`

```html
<p id="p1"></p>
<p id="p2"></p>
<p id="p3"></p>
```

`<p>``id`

`id``id``id`

`id``#` URL `https://foo.com/index.html#bar``bar`

### class

`class``class`

```html
<p class="para"></p>
<p></p>
<p class="para"></p>
```

`<p>``<p>``class`

 class

```html
<p class="p1 p2 p3"></p>
```

`p``p1``p2``p3` class

### title

`title``title`

```html
<div title="">
  <p></p>
</div>
```

`title`

### tabindex

 Tab  Tab 

 Tab HTML `tabindex` Tab index

`tabindex` Tab 

-  JavaScript `focus()` Tab `-1`
- `0` Tab 
- 123…… Tab `tabindex`

```html
<p tabindex="0"></p>
```

`<p>``tabindex``0` Tab 

`tabindex``0``tabindex` Tab `<span>``<div>``tabindex`

### accesskey

`accesskey`

```html
<button accesskey="s"></button>
```

`<button>``s`

`accesskey`“ + ”Chrome  Windows  Linux `Alt + ` Mac `Ctrl + Alt + `

`accesskey`

### style

`style` CSS  CSS 

```html
<p style="color: red;">hello</p>
```



### hidden

`hidden`

```html
<p hidden></p>
```

`p`

CSS `hidden` CSS `hidden`

### langdir

`lang`

```html
<p lang="en">hello</p>
<p lang="zh"></p>
```

`<p>``lang``<p>``lang`

`lang` [BCP47](https://www.ietf.org/rfc/bcp/bcp47.txt) 

- zh
- zh-Hans
- zh-Hant
- en
- en-US
- en-GB
- es
- fr

`dir`

- `ltr`
- `rtl`
- `auto`

### translate

`translate`

```html
<p>
  <span translate="no">Wien<span>
  named world's most liveable city (again)!
</p>
```

`translate="no"``<span>`

`translate``yes`

### contenteditable

HTML `contenteditable`

- `true`
- `false`

```html
<p contenteditable="true">

</p>
```





### spellcheck

`spellcheck`



- `true`
- `false`

```html
<p contenteditable="true" spellcheck="true">
 separate  seperate
</p>
```

`seperate`

`contenteditable`



### `data-`

`data-``data-`

```html
<a href="#" class="tooltip" data-tip="this is the tip!"></a>
```

`data-tip`

`data-` CSS  JavaScript  CSS 

```css
/* HTML 
<div data-role="mobile">
Mobile only content
</div>
*/
div[data-role="mobile"] {
  display:none;
}

/* HTML 
<div class="test" data-content="This is the div content">test</div>​
*/
.test {
  display: inline-block;
}
.test:after {
  content: attr(data-content);
}
```

### 

event handler JavaScript  JavaScript 

> onabort, onautocomplete, onautocompleteerror, onblur, oncancel, oncanplay, oncanplaythrough, onchange, onclick, onclose, oncontextmenu, oncuechange, ondblclick, ondrag, ondragend, ondragenter, ondragexit, ondragleave, ondragover, ondragstart, ondrop, ondurationchange, onemptied, onended, onerror, onfocus, oninput, oninvalid, onkeydown, onkeypress, onkeyup, onload, onloadeddata, onloadedmetadata, onloadstart, onmousedown, onmouseenter, onmouseleave, onmousemove, onmouseout, onmouseover, onmouseup, onmousewheel, onpause, onplay, onplaying, onprogress, onratechange, onreset, onresize, onscroll, onseeked, onseeking, onselect, onshow, onsort, onstalled, onsubmit, onsuspend, ontimeupdate, ontoggle, onvolumechange, onwaiting

