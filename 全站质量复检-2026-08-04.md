# 全站质量复检报告（2026-08-04）

> 复检范围：本次会话 5 个 KB 增值批次（着陆页 / 搜索 / 互链 / SEO / 性能）落地后，全站质量快照。

## 一、门禁状态（三道全绿）

| 门禁 | 结果 | 说明 |
|---|---|---|
| `npm run build`（onBrokenLinks:throw） | ✅ PASS | 客户端/服务端编译成功，零断链，未触发 throw |
| `npm run check:links` | ✅ PASS | 扫描 516 个 HTML，内部链接全部有效；源→产物 368 页齐全；分类落地页齐全；无空字节残留 |
| `node scripts/content-audit.js --gate` | ✅ PASS | docs/docs 零未完成标记（零破窗） |

## 二、页面规模

| 维度 | 数量 |
|---|---|
| docs/docs 源文件（.md/.mdx，含 index） | 294 |
| docs/read 源文件 | 74 |
| blog 文章（.md） | 41 |
| 总内容页（排除 index） | 313 |
| 着陆页 index 文件 | 55 |
| `_category_.json` | 49 |
| 构建产物 HTML | 516 |
| sitemap URL | 515 |

## 三、5 个增值批次验证（均无回归）

| 批次 | 验证指标 | 结果 |
|---|---|---|
| 分类着陆页缺口（39d268d） | 49 个 `_category_.json` 均含 description | ✅ 全覆盖 |
| 搜索体验微调（3a9b4b1） | 结果 12 条 / 路径显示 / 80 字预览 / 索引哈希缓存 `?_=c6a76eb0` | ✅ 生效 |
| 博客→知识库互链（50788ba） | 30/41 篇博客注入「延伸阅读（知识库）」 | ✅ 生效 |
| 全站 SEO 增强（c30e8c8） | og:site_name 注入 / JSON-LD 注入 / sitemap 优先级 6 级梯度 | ✅ 生效 |
| 首页性能优化（369f4e6） | 119 页 lazy-load（logo 已排除）/ KaTeX CSS 延迟 / 3 preload 标签 | ✅ 生效 |

### SEO / 性能细分指标

- **sitemap priority 分布**：`1.0`×1（首页）、`0.8`×1（docs 根）、`0.7`×294（docs 内容页）、`0.6`×206（blog+read）、`0.4`×6（其他）、`0.3`×7（tech-daily 日报）
- **sitemap changefreq 分布**：`monthly`×374、`weekly`×132、`daily`×9
- **空字节复核**：全文搜索 build 下 .html/.js/.json，`\x00` 命中 **0 文件**（strip-null 持续有效）
- **lazy img**：127 个 HTML 文件含 `loading="lazy"`（内容图片，navbar logo 已正确排除）

## 四、已知非缺陷项（误报排除）

- content-audit 列出的 9 个「短页面」（13~83 字）：均为目录着陆页 `index.mdx` 导语（如 `docs/read/社科历史/index.mdx`、`编程语言/Go/并发模型/index.mdx`），非内容页，不含破窗标记，属预期。
- 图片无 alt 2 处：均为 `编程语言/Web前端/HTML/image.md` 的 `<img>` 语法示例（按约定不加 alt，非缺陷）。

## 五、待办

- **6 个本地提交待推送**：`b30ddf2`（上轮移动端适配）+ `39d268d` → `369f4e6`（本次 5 个增值）。
- 推送需先 `git fetch` → `rebase`（合并 CI 每日 tech-daily 提交）→ 一次性 token URL push。
- **需新 GitHub classic PAT**（旧 `ghp_...OHpPj` 已多次明文暴露，须去 Settings → Developer settings → PAT 撤销并轮换，勾 repo）。
