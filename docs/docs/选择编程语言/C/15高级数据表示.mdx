---
sidebar_position: 15
title: 
---

## 

-  malloc 
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/eba20792779d9d9a99e7d4ff0d23ef13.png)

### 

![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/44a5722c917071e937da435fe5695a26.png)

```c showLineNumbers
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 1.
typedef struct node{
    int data;
    struct node *next;
}Node;
int main()
{

    // 2.
    Node a;
    Node b;
    Node c;

    // 3.
    a.data = 1;
    b.data = 3;
    c.data = 5;

    // 4.
    a.next = &b;
    b.next = &c;
    c.next = NULL;

    // 5.
    Node *head = &a;

    // 6.
    while(head != NULL){
        int currentData = head->data;
        printf("currentData = %i\n", currentData);
        head = head->next;
    }
    return 0;
}
```

### 

- 
- 

- 

* ,  next  NULL
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/21a038cfb75cce739e0f1d333126db74.png)

```c showLineNumbers
#include <stdio.h>
#include <stdlib.h>

// 1.
typedef struct node{
    int data;
    struct node *next;
}Node;
int main()
{
    Node *head = createList();
    return 0;
}
// 
Node *createList(){
    // 1.
    Node *node = (Node *)malloc(sizeof(Node));
    if(node == NULL){
        exit(-1);
    }
    // 2.NULL
    node->next = NULL;
    // 3.
    return node;
}
```

- 

* ,  next  NULL
  ![](https://images.weserv.nl/?url=https://img-blog.csdnimg.cn/img_convert/4f238221615ee2ef930526bd53cdc0ef.png)

### 

- 1.
- 2.

```c showLineNumbers
#include <stdio.h>
#include <stdlib.h>

// 1.
typedef struct node{
    int data;
    struct node *next;
}Node;
Node *createList();
void printNodeList(Node *node);
int main()
{
    Node *head = createList();
    printNodeList(head);
    return 0;
}
/**
 * @brief createList 
 * @return  
 */
Node *createList(){
    // 1.
    Node *head = (Node *)malloc(sizeof(Node));
    if(head == NULL){
        return NULL;
    }
    head->next = NULL;

    // 2.
    int num = -1;
    printf("\n");
    scanf("%i", &num);

    // 3.
    while(num != -1){
        // 3.1
        Node *cur = (Node *)malloc(sizeof(Node));
        cur->data = num;

        // 3.2
        cur->next = head->next;
        // 3.3
        head->next = cur;

        // 3.4
        scanf("%i", &num);
    }

    // 3.
    return head;
}
/**
 * @brief printNodeList 
 * @param node 
 */
void printNodeList(Node *node){
    Node *head = node->next;
    while(head != NULL){
        int currentData = head->data;
        printf("currentData = %i\n", currentData);
        head = head->next;
    }
}
```

### 

- 1.
- 2.
- 3.

```c showLineNumbers
#include <stdio.h>
#include <stdlib.h>

// 1.
typedef struct node{
    int data;
    struct node *next;
}Node;
Node *createList();
void printNodeList(Node *node);
int main()
{
    Node *head = createList();
    printNodeList(head);
    return 0;
}
/**
 * @brief createList 
 * @return  
 */
Node *createList(){
    // 1.
    Node *head = (Node *)malloc(sizeof(Node));
    if(head == NULL){
        return NULL;
    }
    head->next = NULL;

    // 2.
    int num = -1;
    printf("\n");
    scanf("%i", &num);

    // 3.
    // 
    Node *pre = head;
    while(num != -1){
        // 3.1
        Node *cur = (Node *)malloc(sizeof(Node));
        cur->data = num;

        // 3.2
        pre->next = cur;
        // 3.3NULL
        cur->next = NULL;
        // 3.4
        pre = cur;

        // 3.5
        scanf("%i", &num);
    }

    // 3.
    return head;
}
/**
 * @brief printNodeList 
 * @param node 
 */
void printNodeList(Node *node){
    Node *head = node->next;
    while(head != NULL){
        int currentData = head->data;
        printf("currentData = %i\n", currentData);
        head = head->next;
    }
}
```

### 

```c showLineNumbers
#include <stdio.h>
#include <stdlib.h>

// 1.
typedef struct node{
    int data;
    struct node *next;
}Node;
Node *createList();
void printNodeList(Node *node);
void insertNode1(Node *head, int data);
void insertNode2(Node *head, int data);
int main()
{
    // 1.
    Node *head = createList();
    // 2.
    insertNode1(head, 1);
    insertNode1(head, 3);
    insertNode1(head, 5);
    printNodeList(head);
    return 0;
}
/**
 * @brief createList 
 * @return  
 */
Node *createList(){
    // 1.
    Node *head = (Node *)malloc(sizeof(Node));
    if(head == NULL){
        return NULL;
    }
    head->next = NULL;
    // 3.
    return head;
}
/**
 * @brief insertNode1 
 * @param head 
 * @param data 
 * @return  
 */
void insertNode1(Node *head, int data){
    // 1.
    Node *pre = head;
    while(pre != NULL && pre->next != NULL){
        pre = pre->next;
    }
    // 2.
    Node *cur = (Node *)malloc(sizeof(Node));
    cur->data = data;

    // 3.
    pre->next = cur;
    // 4.NULL
    cur->next = NULL;
    // 5.
    pre = cur;
}
/**
 * @brief insertNode1 
 * @param head 
 * @param data 
 * @return  
 */
void insertNode2(Node *head, int data){
    // 1.
    Node *cur = (Node *)malloc(sizeof(Node));
    cur->data = data;

    // 2.
    cur->next = head->next;
    // 3.
    head->next = cur;
}
/**
 * @brief printNodeList 
 * @param node 
 */
void printNodeList(Node *node){
    Node *head = node->next;
    while(head != NULL){
        int currentData = head->data;
        printf("currentData = %i\n", currentData);
        head = head->next;
    }
}
```

### 

```c showLineNumbers
/**
 * @brief destroyList 
 * @param head 
 */
void destroyList(Node *head){
    Node *cur = NULL;
    while(head != NULL){
        cur = head->next;
        free(head);
        head = cur;
    }
}
```

### 

```c showLineNumbers
/**
 * @brief listLength 
 * @param head 
 * @return 
 */
int listLength(Node *head){
    int count = 0;
    head = head->next;
    while(head){
       count++;
       head = head->next;
    }
    return count;
}
```

### 

```c showLineNumbers
/**
 * @brief searchList 
 * @param head 
 * @param key 
 * @return
 */
Node *searchList(Node *head, int key){
    head = head->next;
    while(head){
        if(head->data == key){
            break;
        }else{
            head = head->next;
        }
    }
    return head;
}
```

### 

```c showLineNumbers
void deleteNodeList(Node *head, Node *find){
    while(head->next != find){
        head = head->next;
    }
    head->next = find->next;
    free(find);
}
```



### 

- 

* “\0”,"Hello""Hello\0"

### 

-  C ,
- ,‘\0’,
- ‘\0’, 
- 

```c showLineNumbers
    char name[9] = "lnj"; //“\0” \0ASCII0
    char name1[9] = {'l','n','j','\0'};
    char name2[9] = {'l','n','j',0};
    // , 0, 
    char name3[9] = {'l','n','j'};
```

- 

```c showLineNumbers
    //, \n
    // \0 
    char name4[] = {'l','n','j'};

     //   "\0", \0
     //    \0
    char name[] = "c\0ool";
     printf("name = %s\n",name);
: c
```

### 

- , 

* 
*  printf  scanf 

- “%s”, 
- ****

* %s  name \0 

```c showLineNumbers
char chs[] = "lnj";
printf("%s\n", chs);
```

- :

* \0 

```c showLineNumbers
char name[] = {'c', 'o', 'o', 'l' , '\0'};
char name2[] = {'l', 'n', 'j'};
printf("name2 = %s\n", name2); // : lnjcool
```
- ****

```c showLineNumbers
char ch[10];
scanf("%s",ch);
```

- :

* , , 
* ch  9 ‘\0’
*  scanf ,,

### 

- C , 

* 
* ,"stdio.h"
* "string.h"
- :puts
- : puts()
- :
- :

* 
* 

- 

* ,  puts("hello %i");

```c showLineNumbers
char ch[] = "lnj";
puts(ch); //: lnj
```

> - puts  printf , printf 
- :gets
- : gets ()
- :

```c showLineNumbers
char ch[30];
gets(ch); // :lnj
puts(ch); // :lnj
```

> - , gets , scanf 
> -  gets 
- 
-  sizeof 

* ,,

```c showLineNumbers
    char name[] = "it666";
    int size = sizeof(name);// \0
    printf("size = %d\n", size); //:6
```
- 

* : strlen()
* :(‘\0’)

```c showLineNumbers
    char name[] = "it666";
    size_t len = strlen(name2);
    printf("len = %lu\n", len); //:5
```
- “\0”

```c showLineNumbers
/**
 *  
 *  @param name 
 *  @return \0
 */
int myStrlen2(char str[])
{
    //    1.
    int length = 0;
    while (str[length] != '\0')
    {
        length++;//1 2 3 4
    }
    return length;
}
/**
 *  
 *  @param name  
 *  @param count 
 *  @return \0
 */
int myStrlen(char str[], int count)
{
//    1.
    int length = 0;
//    2.
    for (int i = 0; i < count; i++) {
//        3.
        if (str[i] == '\0') {
            return length;
        }
        length++;
    }
    return length;
}
```
- :strcat
- : strcat( 1, 2)
- : 2  1 , 1  “\0” 1 

```c showLineNumbers
char oldStr[100] = "welcome to";
char newStr[20] = " lnj";
strcat(oldStr, newStr);
puts(oldStr); //: welcome to lnj"
```

> - , 1  ,
- :strcpy
  `- : strcpy(1,2)
`- : 2  1 “\0” 2, 

```c showLineNumbers
char oldStr[100] = "welcome to";
char newStr[50] = " lnj";
strcpy(oldStr, newStr);
puts(oldStr); // :  lnj // 
```

> -  1 ,
- :strcmp
- : strcmp( 1, 2)
- : ASCII ,

*  1= 2,=0;
*  1`>` 2,`>`0;
*  1`<` 2,`<`0

```c showLineNumbers
    char oldStr[100] = "0";
    char newStr[50] = "1";
    printf("%d", strcmp(oldStr, newStr)); //:-1
    char oldStr[100] = "1";
    char newStr[50] = "1";
    printf("%d", strcmp(oldStr, newStr));  //:0
    char oldStr[100] = "1";
    char newStr[50] = "0";
    printf("%d", strcmp(oldStr, newStr)); //:1
```

