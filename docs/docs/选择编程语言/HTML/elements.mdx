---
sidebar_position: 18
---
# 



## `<dialog>`

### 

`<dialog>`

```html
<dialog>
  Hello world
</dialog>
```



`open`

```html
<dialog open>
  Hello world
</dialog>
```

`Hello world`

`<dialog>` HTML 

```html
<dialog open>
  <form method="dialog">
    <input type="text">
    <button type="submit" value="foo"></button>
  </form>
</dialog>
```



`<form>``method``dialog``returnValue` Submit `value``foo`

### JavaScript API

`<dialog>` JavaScript API `Dialog.showModal()``Dialog.close()`/

```javascript
const modal = document.querySelector('dialog');

//  open 
modal.showModal();

//  open 
modal.close();
```

`Dialog.close()`

`Dialog.close()``<dialog>``returnValue``returnValue``value`

```javascript
modal.close('Accepted');
modal.returnValue // "Accepted"
```

`Dialog.showModal()`CSS  Dialog `::backdrop`

```css
dialog {
  padding: 0;
  border: 0;
  border-radius: 0.6rem;
  box-shadow: 0 0 1em black;
}

dialog::backdrop {
  /* make the backdrop a semi-transparent black */
  background-color: rgba(0, 0, 0, 0.4);
}
```

`<dialog>`

`<dialog>``Dialog.show()`

### 

`<dialog>`

- `close`
- `cancel``esc`



```javascript
modal.addEventListener('click', (event) => {
  if (event.target === modal) {
    modal.close('cancelled');
  }
});
```

## `<details>``<summary>`

### 

`<details>`

```html
<details>

</details>
```

`Details`

```html
 Details
```



```html
 Details

```



`<details>``open`

```html
<details open>

</details>
```



`<summary>`

```html
<details>
  <summary></summary>
  
</details>
```



```html
 
```



```html
 

```

 CSS `summary::-webkit-details-marker`

```css
summary::-webkit-details-marker {
  background: url(https://example.com/foo.svg);
  color: transparent;
}
```



```css
summary::-webkit-details-marker {
  display: none;
}
summary:before {
  content: "\2714";
  color: #696f7c;
  margin-right: 5px;
}
```


### JavaScript API

`Details``open``<details>`

```javascript
const details = document.querySelector('details');

if (detail.open === true) {
  // 
} else {
  // 
}
```

`Details``toggle`

```javascript
details.addEventListener('toggle', event => {
  if (details.open) {
    /*  */
  } else {
    /*  */
  }
});
```

## 

- [Meet the New Dialog Element](https://keithjgrant.com/posts/2018/meet-the-new-dialog-element/), Keith J. Grant
- [The dialog element: The way to create tomorrow’s modal windows](https://blog.logrocket.com/the-dialog-element-the-way-to-create-tomorrows-modal-windows-f1d4ab14380b), Abhishek Jakhar
- [Details/Summary is the Easiest Way Ever to Make an Accordion](https://css-tricks.com/quick-reminder-that-details-summary-is-the-easiest-way-ever-to-make-an-accordion/), Chris Coyier

