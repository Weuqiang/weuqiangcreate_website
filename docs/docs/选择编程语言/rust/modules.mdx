# 

RustCargoNodeNPM



## 

### cargo new

``` shell
cargo new <xxx> # create bin template project
cargo new <xxx> --lib # create lib template project
```



### cargo build

``` shell
cargo build # build all binary crates
cargo build --bin <xxx> # build specific binary crate
```



### cargo run

``` shell
cargo run # build and running, binary crate
cargo run --bin <xxx> # build and running
```



### cargo publish

Rust`cargo login``cargo publish`



### cargo install

`cargo install``cargo build --release`





## Binary/Library Crate

`Cargo.toml`RustRustRustCrate**Library Crate****Binary Crate**

`src/main.rs`Binary Crate`main`**CrateRust**`src/lib.rs`Library Crate**CrateRust**`Cargo.toml`Crate

![image-20230323203500145](./image-20230323203500145.png)

RustLibrary Crateakara`src/other_file.rs`Binary Crateakaraonetwo`cargo build``cargo run``--lib <xxx>`Crate



## 

RustCrateCrate****`src/main.rs``src/lib.rs`****

`src/lib.rs``One``Two``Three`

``` shell
crate
  - One
  - Two
  - Three
```

`crate``src/lib.rs`**`mod`**`mod Apple`Apple

1. 

   ``` rust
   mod Apple {
   	pub fn A() {
       println!("A");
     }
     pub fn B() {
       println!("B");
     }
     pub fn C() {
       println!("C");
     }
   }
   ```

2. `src/Apple.rs`

3. `src/Apple/mod.rs`

`crate::Apple::B``crate`Crate`std::fs::read`Library Crate`akara::utils::xxx`

``` 
crate
  - One
  - Two
  - Three
  - Apple
    - A
    - B
    - C
```



## 

Rust`pub`

``` rust
pub fn hello() {}

pub mod Apple;
```



## use

`crate::Apple::B`CrateRust**`use`**

``` rust title="src/main.rs"
mod Apple;
use crate::Apple::B;

fn main() {
  B();
  B();
  B();
}
```

