# (CI)

`Jenkins``travis-ci``Github Action``travis-ci``Github Action`

## Github Action

`Github Action``.github/workflows/index.yml``yaml`

`workflow``workflow``job``job``step``step``action`

``` yaml
# ./.github/workflows/npm-publish.yml 

name: Node.js Package

on: # 
  push:
    branches:
      - master # masterpush
jobs:
  build: # job
    runs-on: ubuntu-latest # 
    steps:
      - uses: actions/checkout@v2 # 
      - uses: actions/setup-node@v2 # node
        with:
          node-version: 14
      - run: npm ci
      - run: npm run test
  publish-npm: # job
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
          registry-url: https://registry.npmjs.org/
      - run: npm ci
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.npm_token}}
  github-page:
    needs: build
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - uses: crazy-max/ghaction-github-pages@v2
          with:
            target_branch: gh-pages
            build_dir: public
          env:
            GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
```

`action``actions/checkout@v2``actions/setup-node@v2``action``action`

CINPMGit`Person Access Token``repo - Settings - Secrets`Token


