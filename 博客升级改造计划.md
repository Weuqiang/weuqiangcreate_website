# 魏强的知识花园 · 改造升级计划（专家评估 + 详细路线图）

> 评估人：高级个人知识博客专家
> 评估基准：基于 GitHub Pages 的 Docusaurus 3 静态博客
> 评估日期：2026-07-24
> 评估方法：通读 `docusaurus.config.js` / `src/theme/custom.css` / `sidebars.js` / `package.json`，抽样内容，核对静态资源与 CI，并结合真实渲染截图。

---

## 一、总体结论

**它已经是一个「良好」的个人知识库，但还远不是我心中的「完美博客」。**

六支分类体系清晰、暖色纸感主题统一、KaTeX/Mermaid/Markmap 工具链齐全、内容体量扎实（6 大支 + 69 篇读书笔记 + 34 篇博客 + 50+ 相片 + 8 个作品），CI 自动部署也已打通。骨架是好的。

但「完美博客」差在**四层**：

1. **品牌没落地**——名字有了，自定义 Logo、favicon、社交分享卡全都用了 Docusaurus 默认值或根本没用。
2. **互动层为零**——没有评论、没有访问统计、没有订阅，读者是「来看完就走」的幽灵。
3. **搜索是空壳**——导航栏搜索用 Algolia DocSearch 却没配 `algolia`，等于打不开。
4. **体验有欠账**——没有夜间模式、没有「最近更新」让首页活起来、相册原图巨大拖慢加载、robots.txt 指向了不存在的路由。

### 评分卡（10 分制）

| 维度 | 评分 | 说明 |
|---|---|---|
| 品牌识别（Logo/favicon/社交卡） | 4 | 站名已定，但 `logo.svg` 全项目零引用，favicon 放错目录，社交卡是默认图 |
| 视觉一致性 & 设计系统 | 7 | 暖色纸感统一；缺夜间模式、纸纹理未用、案例页曾崩（已修） |
| 信息架构 & 导航 | 7 | 六支清晰；缺「关于我/最近更新」，搜索失效，footer 含占位链接 |
| 内容体量 & 质量 | 7 | 6 支 + 69 书 + 34 博；少数文章偏短，`static/roadmaps/*.md` 半成品未接入 |
| 阅读体验（TOC/代码/公式/图） | 8 | KaTeX/Mermaid/Markmap/代码高亮齐全，是最大亮点 |
| 互动（评论/统计/订阅） | 1 | 全无 |
| 搜索 | 2 | Algolia 未配，基本不可用；个人站应改本地搜索 |
| 性能 | 5 | 相册巨图（最大 5792×4344）未优化，MUI 重包，live-codeblock 或冗余 |
| SEO & 社交 | 4 | OG 基础有但社交卡默认，keywords 通用，robots 路由错，静态 sitemap 覆盖 |
| 可维护性 & 部署 | 7 | CI 可用；`onBrokenLinks` 仅 warn 无门禁，缺 link-check |
| 可访问性（a11y） | 5 | 对比度尚可；缺统一 focus 样式、skip-link、alt 强约束 |
| 移动端响应式 | 7 | 基本 OK（案例页 `nowrap` 溢出已修） |

**综合约 5.6 / 10。** 结论：骨架优秀，体验与品牌待补，互动层从零起步。

---

## 二、现状体检（事实依据）

### 已做对的事（亮点，必须保留）
- **六支知识体系**（`docs/docs` 下：人工智能/嵌入式开发/数学基础/编程外的基础/编程语言/软件工程与后端）分类清晰，sidebar 自动生成。
- **双 docs 插件** `read`（书架，69 篇按 4 体裁归类）与 `docs`（开发）并存，路由 `/read`、`/docs` 分工明确。
- **暖色人文纸感主题**：陶土棕主色阶 + 米色纸底 + 墨色文字，`custom.css` 设计 token 完整（含阴影、边框、选中色、引用块、代码块、表格、图片圆角）。
- **公式/图表工具链**：`remark-math`+`rehype-katex`（KaTeX 自托管）、`theme-mermaid`、`MarkmapHooks.js`（思维导图）齐备。
- **首页** hero + 五张入口卡片，eyebrow/h1 已分开（「笔记·代码·思考·生活」/「魏强的知识花园」）。
- **CI 自动部署**：`.github/workflows/updata.yml` 在 push `main` 时 `npm ci && npm run build` 后经 peaceiris 推 `gh-pages`。
- **导航品牌已修**：上次提交 `3394a82` 已让 navbar 显示「魏强的知识花园」。

### 问题清单（按优先级）

**P0（影响可用性 / 品牌形象，应先修）**
1. **搜索栏是空壳**：`navbar` 有 `type:"search"`，但 `themeConfig` 无 `algolia` 配置 → DocSearch 无索引，点击搜索弹窗无结果或报错。个人站应换本地搜索。
2. **favicon 放错目录**：`docusaurus.config.js` 写 `favicon: "favicon.ico"`，但文件在 `static/img/favicon.ico`（应为 `static/favicon.ico`）→ 生产环境 favicon 404，且默认是 Docusaurus 火箭图，非品牌。
3. **自定义 `logo.svg` 零引用**：导航栏只用文字标题，未用 `static/img/logo.svg`；favicon 也未用它。品牌资产浪费。
4. **社交分享卡是默认图**：`themeConfig.image: "/img/docusaurus-social-card.jpg"`，文件名沿用 Docusaurus 默认命名，社交平台分享时是未定制的默认模板卡。
5. **footer 占位链接**：微信 `weixin://contacts/profile/wxai2411`（桌面端无效深链）、邮箱 `weuqiang@example.com`（example.com 占位域名）→ 生产环境指向无效地址。
6. **robots.txt 路由写错**：`Allow: /bookshelf/`、`/cases/` 指向不存在的路由（真实是 `/read/`、`/case/`），对 SEO 抓取无意义甚至误导。

**P1（体验与增长，应做）**
7. **无夜间/暗色模式**：`colorMode.disableSwitch: true` 且 `respectPrefersColorScheme: false` → 夜间读者被强光白底冲刷，对「阅读型」博客是硬伤。
8. **首页是静态的**：无「最近更新 / 最新博文」，站点像宣传册不像「活的花园」。
9. **无「关于我」页**：个人博客缺作者身份与可信度锚点，导航无 About。
10. **无访问统计**：完全不知道谁来看、看什么。
11. **无评论系统**：读者无法互动，内容无法沉淀讨论（GitHub Pages 最适配 Giscus）。
12. **`static/roadmaps/*.md` 半成品**：7 个 `learning_roadmap_layerN.md` 裸散在 static，未接入任何页面，属未完成功能残留。
13. **`static/img` 残留默认/演示图**：`undraw_docusaurus_*.svg`、`docusaurus.png`、`banana.JPG`、`sameorigin.PNG`、`samesite.PNG`、`tld.png`、`视频.png` 等多为未用默认/演示资源，应清理。

**P2（打磨与长期）**
14. **相册原图巨大未优化**：`static/pages/gallery/` 有 5792×4344、4524×2112 等巨图，移动端加载慢；无响应式 `srcset`/缩略图。
15. **Gallery 依赖 MUI 重包**：`@mui/material`+`@emotion` 仅 Gallery 的筛选面板使用，约 150KB+，可换轻量原生控件。
16. **`theme-live-codeblock` 可能冗余**：若全站无实时代码块，应禁用以减包。
17. **`onBrokenLinks: 'warn'`**：应升为 `'throw'`，让 CI 把死链当失败，防止上线坏链。
18. **SEO keywords 通用**：`metadata.keywords` 为 `"docusaurus,blog, python, 开源"`，应反映真实内容（编程/嵌入式/AI/数学/读书笔记）。
19. **静态 `sitemap.xml` 覆盖生成**：`static/sitemap.xml` 会覆盖插件生成的实时 sitemap，应删掉让插件生成。
20. **无 404 页**：GitHub Pages 缺自定义 `notFound` 组件，出错体验差。
21. **无「在 GitHub 编辑此页」链接**：`themeConfig` 未配 `editUrl`，读者/作者无法快速纠错。
22. **KaTeX 字体 21 个 woff2 全量自托管**：可接受，但可评估 `font-display: swap` 与子集化。

---

## 三、我心中的「完美个人知识博客」标准

```
完美博客 =
  清晰的身份（Logo / favicon / 社交卡 / 关于我，一眼认出是你）
+ 一致的视觉（设计 token 贯穿全站，含夜间模式，舒服地读）
+ 顺的导航（六支一目了然，搜索真的能搜，最近更新让站点活着）
+ 好的阅读（TOC / 代码 / 公式 / 图 / 进度条 / 复制按钮）
+ 能互动（评论 + 统计 + 订阅，读者留下痕迹）
+ 快且可达（图片优化、a11y、移动端、SEO）
+ 自运营（CI 质量门禁、link-check、内容完成度标注）
```

当前在「清晰的身份」「能互动」「搜索」三块是短板，其余在中上水平。

---

## 四、改造路线图（分 6 个 Phase，由止血到完美）

> 每个 Phase 给出：目标、具体任务（含文件与关键代码）、验收标准。
> 所有改动遵循项目既有约定：**本地提交、不主动推送**（除非你明确要求）。

### Phase 0 · 品牌与可用性的止血（P0，约 0.5 天）

**目标**：把明显错误与品牌真空先补上，让站点的「门面」专业起来。

1. **修复 favicon**
   - 将 `static/img/favicon.ico` 移到 `static/favicon.ico`。
   - 用 `static/img/logo.svg` 生成品牌 favicon（可导出 16/32/48/180 多尺寸 `.ico`，或直接在 config 引用 svg）：
     ```js
     // docusaurus.config.js
     favicon: "favicon.ico",          // 放到 static/favicon.ico
     // 或现代浏览器直接用 svg：
     // favicon: "img/logo.svg",
     ```
   - 验收：`curl -I https://weuqiang.github.io/weuqiangcreate_website/favicon.ico` 返回 200。

2. **导航栏启用 Logo**
   ```js
   navbar: {
     title: siteTitle,
     logo: {
       alt: "魏强的知识花园",
       src: "img/logo.svg",
       srcDark: "img/logo.svg",   // 若有暗色版
       width: 32, height: 32,
     },
     items: [ /* 不变 */ ],
   }
   ```
   - 验收：navbar 左侧出现 Logo + 站名。

3. **定制社交分享卡**
   - 用 `logo.svg` + 站名 + 陶土色纸底，做一张 **1200×630** 的 `og-image.jpg`（可用 Docusaurus 自带 `@docusaurus/plugin-sitemap` 之外，手动出图；或用 `docusaurus-plugin-suggested-twitter-image`/自定义脚本）。
   - 放到 `static/img/og-image.jpg`，改 `themeConfig.image: "/img/og-image.jpg"`。
   - 验收：在 [opengraph.xyz](https://www.opengraph.xyz) 粘贴文章 URL，预览为品牌卡。

4. **修复 footer 占位链接**
   - 微信：若确要放，改用可扫码图片或公众号名；否则删除该无效深链。
   - 邮箱：改为真实邮箱（如 `mailto:你的真实邮箱`）。
   - 验收：footer 三个链接点击均有效或已移除无效项。

5. **修正 robots.txt**
   ```text
   Allow: /read/
   Allow: /case/
   Allow: /gallery/
   # 删除 /bookshelf/ 与 /cases/
   ```
   - 验收：`curl` 拉取 robots.txt，路由与真实一致。

6. **搜索改为本地搜索（关键）**
   - 移除对 Algolia 的依赖，安装并配置本地搜索（Docusaurus 3 兼容的 `@easyops-cn/docusaurus-search-local`）：
     ```bash
     npm i @easyops-cn/docusaurus-search-local
     ```
     ```js
     // docusaurus.config.js plugins 中增加（注意：需从 preset 关闭默认 search 或并存）
     [
       "@easyops-cn/docusaurus-search-local",
       {
         indexBlog: true,
         indexDocs: true,
         docsRouteBasePath: ["docs", "read"],  // 两个 docs 插件都索引
         zhUserDictionary: undefined,
         language: ["zh"],
         highlightSearchTermsOnTargetPage: true,
         searchBarPosition: "right",
       },
     ],
     ```
   - 同时把 navbar 的 `{ type: "search" }` 保留（该插件会接管为本地搜索框）。
   - 验收：构建后首页搜索框输入关键词（如「线性代数」）能弹出本地结果，覆盖 docs/read/blog。

**Phase 0 验收**：favicon/Logo/社交卡就位；搜索可用；footer/robots 正确；`npm run build` 零警告。

---

### Phase 1 · 视觉深化与夜间模式（P1，约 1 天）

**目标**：把「暖色纸感」做成贯穿全站的、含夜间模式的完整设计系统，并让纸纹理真正用起来。

1. **启用夜间模式（夜间阅读）**
   - 在 `custom.css` 增加 `[data-theme="dark"]` 调色板（由现有陶土/纸色推导出低对比暗色）：
     ```css
     [data-theme="dark"] {
       --paper: #211b15;
       --paper-2: #2a221a;
       --ink: #f3e9da;
       --ink-soft: #c3b3a0;
       --line: #3a2f24;
       --highlight-color: #322619;
       --ifm-background-color: var(--paper);
       --ifm-background-surface-color: var(--paper-2);
       --ifm-color-content: var(--ink);
       --ifm-color-content-secondary: var(--ink-soft);
       --ifm-border-color: var(--line);
       --shadow-sm: 0 1px 3px rgba(0,0,0,0.4);
       --shadow-md: 0 8px 24px rgba(0,0,0,0.5);
     }
     /* 暗色下公式/代码块微调 */
     [data-theme="dark"] .prism-code { background: #1c1712; }
     [data-theme="dark"] .markdown code { background: #2e2519; }
     ```
   - 改 `docusaurus.config.js`：
     ```js
     colorMode: {
       defaultMode: "light",
       disableSwitch: false,        // 允许用户切换
       respectPrefersColorScheme: true,  // 跟随系统
     },
     ```
   - 验收：右上角出现日/月切换；切到暗色全站（含代码块、卡片、案例页）协调无刺眼。

2. **纸纹理真正用起来**
   - `static/img/noise.png` 已存在但没用。给 `<body>` 叠一层极淡噪点：
     ```css
     body::before {
       content: "";
       position: fixed; inset: 0; z-index: 0;
       pointer-events: none;
       opacity: 0.035;
       background-image: url("/img/noise.png");
     }
     ```
   - 验收：肉眼可见极淡纸纹，不抢内容。

3. **统一 focus 可见样式（a11y）**
   ```css
   :focus-visible { outline: 2px solid var(--ifm-color-primary); outline-offset: 2px; }
   ```
   - 验收：键盘 Tab 遍历，焦点环清晰。

4. **清理 static/img 残留默认图**
   - 删除 `undraw_docusaurus_*.svg`、`docusaurus.png`、`banana.JPG`、`sameorigin.PNG`、`samesite.PNG`、`tld.png`、`视频.png`（确认未被引用后）。
   - 验收：`grep -rn "undraw_docusaurus\|banana.JPG" src/ docusaurus.config.js` 无引用。

5. **接入或删除 `static/roadmaps` 半成品**
   - 方案 A（推荐）：把 7 个 roadmap 做成一个「学习路线图」页面（在 `src/pages/roadmap.js` 用 Markmap 渲染，复用 `MarkmapHooks`）。
   - 方案 B：暂不上，移出 static 避免被当静态资源。
   - 验收：要么有页面可访问 `/roadmap`，要么资源已清理。

**Phase 1 验收**：明暗两套主题协调；纸纹可见；焦点可见；无残留默认图。

---

### Phase 2 · 首页活化与导航补全（P1，约 1 天）

**目标**：让首页「活」起来，补上「关于我」与「最近更新」。

1. **首页增加「最近更新」区块**
   - 新增 `src/pages/RecentUpdates.js`（或并入 `index.js`），读取 Docusaurus 数据插件暴露的 blog/docs 最近项：
     ```jsx
     import useDocusaurusContext from "@docusaurus/useDocusaurusContext";
     // 通过 @docusaurus/plugin-content-blog 的 useBlogData / useDocsData 取最近 N 篇
     ```
   - 简单稳妥做法：在构建期由脚本生成 `recent.json` 注入，或用 `useBlogData()` 取 `blogPosts` 排序取前 5，配 `useDocsSidebar` 取更新时间最近的 docs（docs 无原生「最近」API，可用 frontmatter `date` 或 git 提交时间近似）。
   - 验收：首页下方展示「最近更新」5 条（标题+日期+链接），随内容自动更新。

2. **新增「关于我」页**
   - `src/pages/about.js` + `about.module.css`，复用 `pageHeader` 风格（eyebrow「关于」/ 标题「关于我」/ 简介 + 技能栈 + 联系方式）。
   - navbar 增加 `{ to: "/about", label: "关于", position: "left" }`。
   - 验收：`/about` 可访问，navbar 出现「关于」。

3. **阅读进度条（可选增强）**
   - 在 `src/theme/BackToTop` 之外，swizzle 一个顶部阅读进度条组件（细线，陶土色），仅文章页显示。
   - 验收：长文滚动时顶部出现进度细线。

4. **「在 GitHub 编辑此页」**
   ```js
   // docusaurus.config.js themeConfig
   editUrl: "https://github.com/Weuqiang/weuqiangcreate_website/edit/main/",
   ```
   - 验收：docs/blog 底部出现「Edit this page」链接，指向正确文件。

**Phase 2 验收**：首页有最近更新；有关于页；编辑链接就位。

---

### Phase 3 · 互动与增长层（P1，约 1–2 天）

**目标**：从「读完就走」变成「能留痕、能回访、能沉淀」。

1. **评论：Giscus（GitHub Discussions 驱动，最适配 GitHub Pages）**
   - 仓库开启 Discussions；在 [giscus.app](https://giscus.app) 生成 snippet。
   - 新建 `src/components/Comments.js`：
     ```jsx
     import React, { useEffect, useRef } from "react";
     export default function Comments({ term }) {
       const ref = useRef(null);
       useEffect(() => {
         const s = document.createElement("script");
         s.src = "https://giscus.app/client.js";
         s.async = true;
         s.setAttribute("data-repo", "Weuqiang/weuqiangcreate_website");
         s.setAttribute("data-repo-id", "<你的 repo id>");
         s.setAttribute("data-category", "Announcements");
         s.setAttribute("data-category-id", "<你的 category id>");
         s.setAttribute("data-mapping", "pathname");
         s.setAttribute("data-reactions-enabled", "1");
         s.setAttribute("data-theme", "preferred_color_scheme");
         s.setAttribute("data-lang", "zh-CN");
         ref.current.appendChild(s);
       }, [term]);
       return <div ref={ref} className="giscus-wrapper" style={{ marginTop: "2rem" }} />;
     }
     ```
   - Swizzle `@theme/DocItem/Footer` 与 blog 文章页底部挂载 `<Comments />`（仅生产环境 `if (process.env.NODE_ENV === 'production')`）。
   - 验收：文章底部出现 Giscus 评论框，登录 GitHub 可评论。

2. **访问统计：隐私友好方案**
   - 推荐 **GoatCounter**（免费、无 cookie、一行脚本）或 **Plausible** / 自托管 **Umami**。
   - GoatCounter 接入：在 `src/theme/Layout` 或 `docusaurus.config.js` 的 `scripts` 加：
     ```js
     scripts: [{ src: "https://gc.zgo.at/count.js", "data-goatcounter": "https://<你的码>.goatcounter.com/count" }],
     ```
   - 验收：GoatCounter 后台能看到访问与热门页面。

3. **RSS / 邮件订阅**
   - 博客 RSS 已配（`feedOptions.type: "all"`），在 navbar/footer 加 RSS 图标链接 `/blog/rss.xml`。
   - 可选：用 GoatCounter 或 Buttondown/Mailchimp 接邮件订阅（P2，按需）。

**Phase 3 验收**：文章可评论；后台有访问数据；RSS 可订阅。

---

### Phase 4 · 性能与 SEO / 可访问性（P2，约 1 天）

**目标**：快、被搜到、人人可用。

1. **相册图片优化**
   - 用脚本（`sharp` 批处理）把 `static/pages/gallery/*.webp` 生成 ≤1600px 宽的主图 + ≤480px 缩略图，Gallery 用 `react-photo-album` 的 `srcset` 能力。
   - 验收：Lighthouse 移动端「最大内容绘制」明显下降；原图不再直出。

2. **Gallery 去 MUI**
   - 筛选面板（分类/排序）用原生 `<select>` + 自写轻量样式替代 `@mui/material`，从 `package.json` 移除 `@mui/material`、`@emotion/react`、`@emotion/styled`（仅 Gallery 用）。
   - 验收：`npm run build` 后 JS 包体积下降；筛选功能不变。

3. **禁用冗余插件**
   - 若全站无实时代码块，`themes` 去掉 `"@docusaurus/theme-live-codeblock"`。
   - 验收：包体再降，功能无损。

4. **SEO 强化**
   - `metadata.keywords` 改为真实词：`个人知识库, 编程, 嵌入式, 人工智能, 数学, 读书笔记, 魏强`。
   - 删 `static/sitemap.xml` 让插件生成实时版；`robots.txt` 的 Sitemap URL 已正确。
   - 进阶：为博客文章注入 JSON-LD（`@type: BlogPosting`），用 swizzle 或社区插件（如 `docusaurus-plugin-schema`）。
   - 验收：提交 `site:weuqiang.github.io/weuqiangcreate_website` 能被搜到；sitemap 含全部 URL。

5. **`onBrokenLinks: 'throw'` + CI link-check**
   - config 改为 `'throw'`；CI 增加一步 `npx linkcheck build` 或 Docusaurus 自带断链即失败。
   - 验收：任一死链导致 `npm run build` 退出非 0，CI 红。

6. **自定义 404 页**
   - `src/pages/404.js` + 风格化「页面走丢了」+ 回首页/文档入口。
   - 验收：访问不存在路径显示品牌 404。

7. **a11y 补充**
   - 所有 `<img>` 强约束 `alt`；装饰图 `alt=""`；`lang="zh-CN"` 已在 html（Docusaurus 默认）。
   - 验收：axe DevTools 无 critical 问题。

**Phase 4 验收**：Lighthouse 性能/SEO/a11y 均 ≥ 90；死链门禁生效；404 友好。

---

### Phase 5 · 内容治理与长期运营（持续）

**目标**：让「花园」持续生长且不出杂草。

1. **内容完成度标注**
   - 在文章 frontmatter 加 `status: draft|writing|done`，用 Admonition 或徽章提示读者（如「🚧 草稿」）。
   - 验收：草稿文首有提示，避免半成品误导。

2. **更新日志 / 变更公告**
   - 启用 `announcementBar`（已留 CSS）发布重大更新；或写 `CHANGELOG.md`。
   - 验收：访问时看到一次性公告。

3. **定期内容审计**
   - 脚本统计：各支文章数、最早/最近更新、草稿占比，输出到 `README` 或 dashboard。
   - 验收：每季度可出一份内容健康度报告。

4. **i18n（如需要）**
   - 当前仅 `zh-Hans`；若想出海，规划 `en` locale（P2，按需）。

---

## 五、可执行任务清单（总表）

| # | 任务 | 关键文件 | 优先级 | 验收 |
|---|---|---|---|---|
| 1 | favicon 归位 + 品牌化 | `static/favicon.ico`, `docusaurus.config.js` | P0 | favicon.ico 返回 200 |
| 2 | navbar 启用 logo.svg | `docusaurus.config.js` | P0 | 左上出现 Logo |
| 3 | 定制社交分享卡 1200×630 | `static/img/og-image.jpg`, config | P0 | opengraph 预览为品牌卡 |
| 4 | 修复 footer 占位链接 | `docusaurus.config.js` | P0 | 链接全部有效 |
| 5 | 修正 robots.txt 路由 | `static/robots.txt` | P0 | 路由与真实一致 |
| 6 | 搜索换本地搜索 | `@easyops-cn/docusaurus-search-local`, config | P0 | 本地搜索出结果 |
| 7 | 夜间模式调色板 | `src/theme/custom.css`, config | P1 | 明暗切换协调 |
| 8 | 纸纹理 noise.png 启用 | `custom.css` | P1 | 极淡纸纹可见 |
| 9 | 统一 focus 样式 | `custom.css` | P1 | Tab 焦点环清晰 |
| 10 | 清理 static/img 残留图 | `static/img/*` | P1 | 无残留默认图引用 |
| 11 | 接入/清理 roadmaps | `src/pages/roadmap.js` 或删除 | P1 | 有页或已清理 |
| 12 | 首页「最近更新」 | `src/pages/index.js` + 新组件 | P1 | 展示最近 5 条 |
| 13 | 关于我页 | `src/pages/about.js` | P1 | `/about` 可访问 |
| 14 | 阅读进度条 | swizzle 组件 | P1 | 长文顶部进度线 |
| 15 | 编辑此页链接 | config `editUrl` | P1 | 底部编辑链接正确 |
| 16 | Giscus 评论 | `src/components/Comments.js` | P1 | 文章可评论 |
| 17 | 访问统计 | GoatCounter/Plausible | P1 | 后台有数据 |
| 18 | RSS 订阅入口 | navbar/footer | P1 | /blog/rss.xml 可订 |
| 19 | 相册图片优化 | `sharp` 批处理 + 缩略图 | P2 | LCP 下降 |
| 20 | Gallery 去 MUI | `src/pages/gallery.js`, `package.json` | P2 | 包体降、功能不变 |
| 21 | 禁用 live-codeblock(若冗余) | config `themes` | P2 | 包体降 |
| 22 | SEO keywords + 去静态 sitemap | config, `static/sitemap.xml` | P2 | 关键词真实、sitemap 实时 |
| 23 | onBrokenLinks→throw + CI link-check | config, `updata.yml` | P2 | 死链致构建失败 |
| 24 | 自定义 404 | `src/pages/404.js` | P2 | 友好 404 |
| 25 | a11y 补充（alt/focus） | 内容 + `custom.css` | P2 | axe 无 critical |
| 26 | 内容完成度标注 | frontmatter `status` | P2 | 草稿有提示 |
| 27 | 公告栏/更新日志 | config `announcementBar` | P2 | 发布一次公告 |

---

## 六、风险与权衡

- **夜间模式**：暖色纸感是核心调性，暗色需谨慎推导低对比陶土暗色，避免「为了暗而暗」破坏气质。若你更想保持纯暖光，可只做 `respectPrefersColorScheme: true` 而保留 disableSwitch，不强推切换按钮。
- **本地搜索 vs Algolia**：本地搜索零成本、离线可用、适合个人站；代价是索引在客户端（首屏 JS 略增）。对当前体量（数百页）完全够用，推荐。
- **Giscus 依赖 GitHub 登录**：访客需 GitHub 账号才能评论，会过滤掉一部分人；但零服务器、与 GitHub Pages 同源，是最省心的方案。可后续叠加「邮件/匿名留言」表单作为补充。
- **统计隐私**：GoatCounter/Plausible 无 cookie、合规；不要用需 cookie 同意的传统 GA，除非你愿加 consent 弹窗。
- **图片优化会改 Gallery 资源**：务必保留原图备份（`git` 已跟踪，安全），缩略图走构建产物或新目录。
- **Phase 0/1 改动 config 与 css，可能影响现有构建**：每步后跑 `npm run build`（项目用 `CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= npm run build` 绕过 safe-delete 门禁）确认零警告。

---

## 七、建议落地顺序

```
第 1 步（必做，止血+品牌）：#1 #2 #3 #4 #5 #6   → 站点专业、能搜
第 2 步（体验）：          #7 #8 #9 #12 #13 #15  → 明暗协调、首页活、有关于
第 3 步（互动增长）：      #16 #17 #18           → 评论+统计+订阅
第 4 步（性能/SEO）：      #19 #20 #21 #22 #23 #24 #25 → 快、被搜、友好
第 5 步（长期）：          #10 #11 #14 #26 #27   → 清理、进度条、治理
```

**最低可行完美（MVP of Perfect）**：先做完第 1 步 + 第 2 步的 #7/#12/#13，站点就会从「良好」跃升到「专业且有呼吸感」；再补第 3 步互动层，就基本达到我心中的「完美博客」门槛。

---

## 八、本次可立即交付的下一步

如果你认可这份计划，我建议**从 Phase 0 开始逐条落地**（不推送，仅本地提交）。你也可以指定某个 Phase 让我直接开工，例如：

- 「先做 Phase 0（品牌+搜索止血）」
- 「先做 Phase 1（夜间模式+纸纹理）」
- 「只做搜索本地化 + favicon 修复」

告诉我从哪一步开始即可。
