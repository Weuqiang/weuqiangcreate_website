---
sidebar_position: 21
title: 调试与性能分析
---

代码写出来只是第一步，能「看清它在做什么、为什么慢」才是工程能力。`timeit` 做微观计时，`cProfile`/`pstats` 定位热点，`tracemalloc` 追踪内存分配，`pdb` 让你逐行走进运行中的程序。掌握它们，调试与优化不再靠「猜」。

## 本章模块

- [性能剖析与调试](./profiling) — timeit、cProfile、tracemalloc 与 pdb 的实战用法
