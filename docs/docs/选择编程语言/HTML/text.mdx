---
sidebar_position: 8
---
# 

HTML 

## `<div>`

`<div>`division

 CSS `<div>`

```html
<div class="main">
  <div class="article">
    <div class="title">
      <h1></h1>
    </div>
  </div>
</div>
```

HTML 5 

```html
<main>
  <article>
    <header>
      <h1></h1>
    </header>
  </article>
</main>
```

`<div>``<div>`

```html
<div>
  <img src="warning.jpg" alt="">
  <p></p>
</div>
```

`<div>``<article>``<section>``<aside>``<nav>``<div>`

## `<p>`

`<p>`paragraph`<p>`

```html
<p>hello world</p>
```



## `<span>`

`<span>` CSS `<span>`

```html
<p><span></span></p>
```

`<span>`

## `<br>``<wbr>`

`<br>`

```html
hello<br>world
```

`hello``world`

`<br>`

```html
<p>
  <br>
  <br>
  <br>
  
</p>
```

`<br>`

`<br>` CSS 

```html
<p></p>
<br>
<br>
<p></p>
```

`<br>` CSS

`<wbr>``<br>``<wbr>` URL 

```html
<p>
Fernstraßen<wbr>bau<wbr>privat<wbr>finanzierungs<wbr>gesetz
</p>
```

`<wbr>`

## `<hr>`

`<hr>`

```html
<p></p>
<hr>
<p></p>
```



`<section>` CSS

## `<pre>`

`<pre>`preformatted

```html
<pre>hello

   world</pre>
```

`<pre>`

HTML `<pre>``<pre>` HTML 

```html
<pre><strong>hello world</strong></pre>
```

`<pre>`

## `<strong>``<b>`

`<strong>`

```html
<p><strong></strong></p>
```

`<b>``<strong>` Boldface 

```html
<p><b></b></p>
```

`<strong>``<strong>`

## `<em>``<i>`

`<em>`emphasize

```html
<p><em></em></p>
```

`<em>` CSS 

`<i>``<em>` Italic 

```html
<p><i></i></p>
```

`<i>``<em>`

## `<sub>``<sup>``<var>`

`<sub>``<sup>`

```html
<p> H<sub>2</sub>O</p>
```

`<var>`

```html
<p>
  <var>a</var><sup>2</sup> + <var>b</var><sup>2</sup> = <var>c</var><sup>2</sup>
</p>
```

## `<u>``<s>`

`<u>`

```html
<p>
<em></em><u></u>
</p>
```

`<u>`“”

`<u>``<u>` CSS `<u>`

`<s>`

```html
<p><s></s></p>
```

“”

## `<blockquote>``<cite>``<q>`

`<blockquote>`

```html
<blockquote cite="https://quote.example.com">
  <p> 1% 99%</p>
</blockquote>
```

`<blockquote>``cite`

`<cite>`

```html
<blockquote cite="https://quote.example.com">
  <p> 1% 99%</p>
</blockquote>
<cite>-- </cite>
```

`<cite>``<blockquote>`

```html
<p><cite></cite></p>
```

`<q>``<blockquote>`

```html
<p>
  
  <q cite="https://quote.example.com"></q>
</p>
```



`<blockquote>``<q>``cite`

`<q>`

## `<code>`

`<code>`

```html
<code>alert()</code>
```

`<code>``<pre>``<code>`

```html
<pre>
<code>
  let a = 1;
  console.log(a);
</code>
</pre>
```

## `<kbd>``<samp>`

`<kbd>`

```html
<p>Windows  <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Del</kbd> </p>
```

`<kbd>`

```html
<p>Windows 
<kbd> <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Del</kbd> </kbd>
</p>
```

`<samp>`

```html
<p>
<samp>Uncaught ReferenceError: foo is not defined</samp>
</p>
```

## `<mark>`

`<mark>`Chrome 

```html
<p><mark></mark></p>
```

`<mark>``<q>``<blockquote>`

```html
<blockquote>
<mark></mark>
</blockquote>
```

`<mark>`

 CSS 

## `<small>`

`<small>` CSS 

```html
<p></p>
<p><small></small></p>
```

## `<time>``<data>`

`<time>`

```html
<p><time datetime="2015-06-10"></time></p>
```

`<time>`

`<time>``datetime`

- `2011`
- `2011-11`
- `2011-11-18`
- `11-18`
- `2011-W47`
- `14:54``14:54:39``14:54:39.929`
- `2011-11-18T14:54:39.929`

```html
<p><time datetime="20:00"></time></p>
```

`<data>``<time>`

```html
<p><data value="39"></data></p>
```

`<data>``value`

## `<address>`

`<address>`

```html
<p></p>

<address>
  <p><a href="mailto:foo@example.com">foo@example.com</a></p>
  <p><a href="tel:+555-34762301">+555-34762301</a></p>
</address>
```



1`<address>`

2`<address>`

3`<address>``<h1>`~`<h6>``<article>``<aside>``<section>``<nav>``<header>``<footer>`

4`<address>``<footer>`

```html
<footer>
  <address>
    <a href="mailto:zhangsan@example.com">
    McClure</a>
  </address>
</footer>
```

## `<abbr>`

`<abbr>``title``title`

```html
<abbr title="HyperText Markup Language">HTML</abbr>
```



## `<ins>``<del>`

`<ins>`insert`<del>`delete

```html
<del><p>58</p></del>
<ins><p>59</p></ins>
```

`<del>``<ins>`



- `cite` URL
- `datetime`

```html
<ins cite="./why.html" datetime="2018-05">
  <p></p>
</ins>
```

## `<dfn>`

`<dfn>`definition

```html
<p>
 TCP/IP  <dfn>Internet</dfn>
</p>
```

`<dfn>``title`

```html
<p>
 TCP/IP 
<dfn title="">Internet</dfn>
</p>
```

`title`

`<dfn>``<abbr>`

```html
<p>
<dfn><abbr title="acquired immune deficiency syndrome">AIDS</abbr></dfn>

</p>
```

## `<ruby>`

`<ruby>`

```html
<ruby>
<rp>(</rp><rt>han</rt><rp>)</rp>
<rp>(</rp><rt>zi</rt><rp>)</rp>
</ruby>
```

```han zi`

`<ruby>`

`<ruby>`

**1`<rp>`**

`<rp>`

`<rp>`

```html
<ruby>
<rp>(</rp><rt>han</rt><rp>)</rp>
<rp>(</rp><rt>zi</rt><rp>)</rp>
</ruby>
```

`(han)(zi)`

**2`<rt>`**

`<rt>`

**3`<rb>`**

`<rb>`

```html
<ruby>
  <rb></rb><rb></rb>
  <rp>(</rp>
  <rt>han</rt>
  <rt>zi</rt>
  <rp>)</rp>
</ruby>
```

```<rb>``<rt>`

Chrome 

**4`<rbc>``<rtc>`**

`<rbc>``<rb>``<rtc>``<rbc>`

```html
<ruby style="ruby-position: under;">
  <rbc>
    <rb></rb><rp>(</rp><rt>han</rt><rp>)</rp>
    <rb></rb><rp>(</rp><rt>zi</rt><rp>)</rp>
  </rbc>
  <rtc style="ruby-position: over;">
    <rp>(</rp><rt>Chinese</rt><rp>)</rp>
  </rtc>
</ruby>
```

```<rbc>``<rtc>``<rbc>``style`

Chrome 

## `<bdo>``<bdi>`

`<bdo>`

```html
<p><bdo dir="rtl"></bdo></p>
```

`<bdo>`“”

`<bdo>``dir``ltr``rtl`

`<bdi>``<bdi>`

```html
<p><bdi></bdi></p>
```

