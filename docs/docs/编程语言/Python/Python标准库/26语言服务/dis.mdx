---
sidebar_position: 1
title: dis
description: "dis 模块反汇编 Python 字节码，帮你看清代码在解释器层面到底怎么执行"
---

## 学习目标

- 理解字节码与 CPython 虚拟机的关系
- 用 `dis.dis` 反汇编函数看指令序列
- 借助 dis 验证语法糖/优化与排查性能

## 前置知识

- Python 函数与基本语法
- 对「解释执行」有概念即可

## 核心概念

`dis` 把编译后的字节码翻译回可读指令。`LOAD_FAST`/`STORE_FAST` 操作局部变量，`BINARY_OP` 做运算，`CALL` 调函数。它不改变运行行为，只是「读」字节码——是理解 Python 底层语义的显微镜，常用于验证某个写法是否真的更优。

# dis — 字节码反汇编

`dis` 是 Python 标准库自带的**字节码反汇编器**。它把编译后的 `.pyc` 字节码翻译回人类可读的指令序列，让你看清一行 Python 代码在 CPython 解释器里**实际是怎么一步步执行的**。

## 为什么要用 dis

Python 是解释执行的，源码先编译成字节码再由虚拟机（VM）逐条执行。`dis` 让你绕过"我以为它会这样跑"的直觉，直接看"它真的这样跑"：

- 验证某个语法糖 / 优化是否生效（比如 `f-string`、`match`）。
- 排查性能问题（数一数某段代码生成了多少条指令）。
- 理解闭包、变量作用域、异常处理的底层机制。

## 基本用法：`dis.dis`

直接把一个函数、方法或代码对象传给 `dis.dis` 即可打印反汇编：

```python showLineNumbers
import dis

def add(a, b):
    return a + b

dis.dis(add)
```

输出会列出每一条指令的**行号、字节码偏移、指令名、参数**。`LOAD_FAST` 读取局部变量，`BINARY_OP` 做二元运算，`RETURN_VALUE` 返回。

## 实战：看 `match` 语句怎么编译

`match`（结构匹配）是新语法，用 `dis` 可以看清它背后就是一系列比较与跳转：

```python showLineNumbers
import dis

def classify(x):
    match x:
        case [a, b]:
            return "二元组"
        case _:
            return "其他"

print(dis.dis(classify))
```

你会发现 `case [a, b]` 并没有真的"拆包魔法"，而是编译成对序列长度、元素取值的一连串判断指令。理解这一点，就不会对 `match` 的性能或行为产生错误预期。

## 更细粒度：`dis.Bytecode`

`dis.Bytecode` 把指令封装成可迭代对象，方便在程序里分析：

```python showLineNumbers
import dis

bc = dis.Bytecode(lambda: [1, 2, 3])
for instr in bc:
    print(instr.opname, instr.argval)
```

## 注意事项

- `dis` 展示的是 **CPython 的实现细节**，不同 Python 版本指令集会变化，不要写依赖具体指令的代码。
- 它反汇编的是字节码，不是源码——变量名、常量值可见，但注释和控制流结构需要你自行推断。
- 想看整个模块的字节码，可以直接 `dis.dis("模块名")` 或对 `.pyc` 文件使用 `dis.disco`。

> 延伸：想理解字节码如何被真正执行，可以结合 `inspect` 模块查看函数的 `__code__` 对象（常量表、变量名表等）。

## 示例

```python
import dis

def greet(name):
    return f"hi {name}"

dis.dis(greet)
# 可看到 LOAD_FAST / FORMAT_VALUE / RETURN_VALUE 等指令
```

## 易错点

- 字节码随 Python 版本变化：不同版本的指令名/数量可能不同，不要写死依赖某条指令。
- `dis` 只「读」不「改」：它不影响运行，看错也只会误导你的理解，不会破坏程序。
- 闭包/生成器反汇编较长：建议用 `dis.dis(func.__code__)` 聚焦函数体。
- 优化差异需实测：`dis` 能验证某写法生成更少指令，但指令少不等于更快（还有解释器开销）。

## 练习

- 对比 `for` 循环与 `map` 的字节码条数。
- 用 dis 验证 `if x: return` 与 `return x or None` 的差异。
- 观察 `f-string` 与 `%` 格式化的指令区别。

## 小结

`dis` 是把 Python「黑盒」打开的显微镜：它让你看到 CPython 实际执行的指令序列。用它验证优化、理解闭包与异常机制，能建立比「我以为」更可靠的底层直觉。

## 延伸阅读
- [返回总览](../../)
