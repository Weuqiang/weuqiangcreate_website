# PostCSS

`postcss`**CSS**`postcss-cli``webpack``postcss``postcss-loader`

`postcss.config.js`

`babel`****

## plugins

### autoprefixer

`PostCSS`CSS

``` css
/*  */
::placeholder {
  color: gray;
}

/*  */
::-moz-placeholder {
  color: gray;
}
:-ms-input-placeholder {
  color: gray;
}
::placeholder {
  color: gray;
}
```

`webpack`

``` js
// webpack.config.js
module.exports = {
    module: {
        rules: [
            {
                test: /\.css$/,
                use: ["style-loader", "css-loader", "postcss-loader"] // postcss-loader
            }
        ]
    },
}
```

``` js
// postcss.config.js
const autoprefixer = require('autoprefixer')

module.exports = {
    plugins: [
        autoprefixer, //  autoprefixer()
    ]
}
```



### postcss-preset-env

`poyfills`

``` js
// postcss.config.js
const env = require('postcss-preset-env')
const autoprefixer = require('autoprefixer')

module.exports = {
    plugins: [
        autoprefixer,
        env({ stage: 0 })
    ]
}
```

### postcss-modules



``` css
/*  */
.name {
  color: gray;
}

/*  */
.Logo__name__SVK0g {
  color: gray;
}
```

`json`



