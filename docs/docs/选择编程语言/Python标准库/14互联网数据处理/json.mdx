
## json 

json  python->json  json->python 

| JSON ->        | Python -> | JSON           |
| -------------- | --------- | -------------- |
| object --   | dict      | object --  |
| array          | list      | array          |
| string         | str       | string         |
| number (int)   | int       | number         |
| number (real)  | float     | number         |
| true           | True      | true           |
| false          | False     | false          |
| null           | None      | null           |
|                | tuple     | array          |

JSON - str 
JSON  JSON  x  loads(dumps(x)) != x

json 

[json ](https://docs.python.org/zh-cn/3/library/json.html#module-json)

### json.load  json.dump

json.load  json.dump 

```python showLineNumbers
import json

data = {
    "name": "Satyam kumar",
    "place": "patna",
    "skills": [
        "Raspberry pi",
        "Machine Learning",
        "Web Development"
    ],
    "email": "xyz@gmail.com",
    "projects": [
        "Python Data Mining",
        "Python Data Science"
    ]
}
with open("data_file.json", "w") as write:
    json.dump(data, write)

with open("data_file.json", "r") as read_content:
    print(json.load(read_content))
```

### json.loads  json.dumps

`json.loads`  `json.dumps` `s``string`()

```python showLineNumbers
import json

# JSON string:
# Multi-line string
data = """{
    "Name": "Jennifer Smith",
    "Contact Number": 7867567898,
    "Email": "jen123@gmail.com",
    "Hobbies":["Reading", "Sketching", "Horse Riding"]
    }"""

# parse data:
res_p = json.loads(data)
print(type(res_p)) # <class 'dict'>

res_j = json.dumps(res_p)
print(type(res_j)) # <class 'str'>
```
