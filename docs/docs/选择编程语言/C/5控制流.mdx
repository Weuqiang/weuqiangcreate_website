---
sidebar_position: 5
title: 
---

C  3 

- 
- 
- 


## 

- C  if  switch

### if  else

- if 

  -  1

```c showLineNumbers
if() {
  1;
}
;
```

```c showLineNumbers
#include <stdio.h>
int main() {
    int age = 20;
    if(age >= 18) {
        printf("18\n");
    }
    printf("if\n");
    return 0;
}
```

- if 
  -  1 2
  - else  if 

```c showLineNumbers
if(){
  1;
}else{
  2;
}
;
```

```c showLineNumbers
#include <stdio.h>
int main() {
    int age = 16;
    // if else 
    if(age >= 18){
        printf("\n");
    }else{
        printf("\n");
    }
    printf("if\n");
    return 0;
}
```

- if 
  - " 1"" 1"" 2"" 2"" 3"" 3" 123  else 
  - 
  -  else 

```c showLineNumbers
if(1) {
  1;
}else if(2){
  2;
}else if(3){
  3;
}else{
  4;
}
;
```

```c showLineNumbers
#include <stdio.h>
int main() {
    int age = 30;
    if(age>40){
        printf("\n");
    }else if(age>25){
        printf("\n");
    }else if(age>18){
        printf("\n");
    }else{
        printf("\n");
    }
    printf("if\n");
    return 0;
}
```

- if 
  - if  ifelse  if

```c showLineNumbers
if(1){
    1;
   if(2){
      2;
  }
}else{
   if(3){
      3;
  }else{
      4;
  }
}
```

- **_if _**
  - 

```c showLineNumbers
#include <stdio.h>
int main(){
    if(0){
        printf("if\n");
    }else{
        printf("else\n"); // 
    }
    return 0;
}
```

-  if else if else 

<Highlight>C `;`</Highlight>

```c showLineNumbers
#include <stdio.h>
int main(){
    if(10 > 2);
    {
        printf("10 > 2\n");
        // : 10 > 2
    }
    
    /*
    if(10 > 2)if
    if
    
    
    */
    if(10 > 2){/*if*/};
    {
        printf("if\n");
        // : if
    }
    return 0;
}
```

-  if else else  if 

```c showLineNumbers
#include <stdio.h>
int main(){
    if(0)
        if(1)
            printf("A\n");
        else // if(1)
            printf("B\n");
    else // if(0), if(1)
        if (1)
            printf("C\n"); // C
        else // if(1)
            printf("D\n");
    return 0;
}
```

-  if else 

```c showLineNumbers
#include <stdio.h>
int main(){
    // if
    // if(1)
    //     int number = 10; // 
    // printf("number = %i\n", number);
    
    // 
    if(1){
        int number = 10;
        printf("number = %i\n", number);
    }
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>
int main(){
    // else
    // if(0){
    //     int number = 10;
    // }else
    //     int value = 20; // 
    // printf("value = %i\n", value);
    
    // 
    if(0){
        int number = 10;
    }else{
        int value = 20;
        printf("value = %i\n", value);
    }
    return 0;
}
```


-  `==`  `=`

```c showLineNumbers
#include <stdio.h>
int main(){
    int a = 8;
    // if(a = 10){// , , 10a
    if (10 == a){ // , 
        printf("a10\n");
    }else{
        printf("a10\n");
    }
    return 0;
}
```

### switch case  default

-  `if...else if...else`  switch  if switch 
- 

```c showLineNumbers
switch(){
    case 1:
        1;
        break;
    case 2:
        2;
        break;
    case n:
        n;
        break;
    default:
        n+1;
        break;
}
```

- 
  - """""""" switch 
  - "" case "" default 
- 

```c showLineNumbers
#include <stdio.h>

int main() {

    int num = 3;
    switch(num){
    case 1:
        printf("\n");
        break;
    case 2:
        printf("\n");
        break;
    case 3:
        printf("\n");
        break;
    case 4:
        printf("\n");
        break;
    case 5:
        printf("\n");
        break;
    case 6:
        printf("\n");
        break;
    case 7:
        printf("\n");
        break;
    default:
        printf("\n");
        break;
    }
    return 0;
}
```

- **switch **

- switch charshort

```c showLineNumbers
#include <stdio.h>

int main() {

    switch(1.1){ // 
    case 1:
        printf("\n");
        break;
    case 2:
        printf("\n");
        break;
    default:
        printf("\n");
        break;
    }
    return 0;
}
```

- case charshort

```c showLineNumbers
#include <stdio.h>

int main() {

    int num = 3;
    switch(1){
    case 1:
        printf("\n");
        break;
    case 'a':
        printf("\n");
        break;
    case num: // 
        printf("\n");
        break;
    case 4.0: // 
        printf("\n");
        break;
    default:
        printf("\n");
        break;
    }
    return 0;
}
```

- case 

```c showLineNumbers
#include <stdio.h>

int main() {
    switch(1){
    case 1: // 
        printf("\n");
        break;
    case 1: // 
        printf("\n");
        break;
    default:
        printf("\n");
        break;
    }
    return 0;
}
```

- case  case 

```c showLineNumbers
#include <stdio.h>

int main() {
    switch(1){
    case 1:{
        int num = 10;
        printf("num = %i\n", num);
        printf("\n");
        break;
        }
    case 2:
        printf("\n");
        break;
    default:
        printf("\n");
        break;
    }
    return 0;
}
```

- switch  case  case  default  case  default  break 

```c showLineNumbers
#include <stdio.h>

int main() {

    int num = 2;
    switch(num){
    case 1:
        printf("\n");
        break;
    case 2:
        printf("\n"); // 
    case 3:
        printf("\n"); // 
    default:
        printf("\n"); // 
        break;
    }
    return 0;
}
```

- switch  default 

```c showLineNumbers
#include <stdio.h>

int main() {
    switch(1){
    case 1:
        printf("\n");
        break;
    case 2:
        printf("\n");
        break;
    }
    return 0;
}
```

- switch  default  case 

```c showLineNumbers
#include <stdio.h>

int main() {
    switch(3){
    case 1:
        printf("\n");
        break;
    default:
        printf("Other,,,\n");
        break;
    case 2:
        printf("\n");
        break;
    }
    return 0;
}
```

- **if  switch **
-  if  switch  if switch 
  - `if...else if...else` 
  - `switch` 
-  100

```c showLineNumbers
#include <stdio.h>

int main() {
    int a = -1;
    scanf("%d", &a);
    if(a > 100){
        printf("100\n");
    }else{
        printf("100\n");
    }
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>

int main() {
    int a = -1;
    scanf("%d", &a);
    // (T)(M)(D)
    switch (a) {
        case 101:
        case 102:
        case 103:
        case 104:
        case 105:
            printf("\n");
            break;
        default:
            printf("\n");
            break;
    }
    return 0;
}
```

## 

- C  whiledo-while  for
- 
  - 
  - """"

### while

- 

```c showLineNumbers
while (   ) {
    ;
    ;
    ....
}
```

- 

  - 
    - 
  - 
    - 
  - 
    - 

- 


```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 0;
    // 1.,0 < 3
    // 4.,1 < 3
    // 7.,2 < 3
    // 10.,3 < 3, 
    while (count < 3) {
        // 2., ""
        // 5., ""
        // 8., ""
        printf("~\n");
        // 3."" count = 1
        // 6."" count = 2
        // 9."" count = 3
        count++;
    }
    // 11., ""
    printf("\n");
    return 0;
}
```

- while 
  - ""
  - """"
  - ""

```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 4;
    // 1.,
    while (count < 3) {
        printf("~\n");
        count++;
    }
    // 2., ""
    printf("\n");
    return 0;
}
```

- **_while _**
  - 

```c showLineNumbers
#include <stdio.h>
int main(){
    // Ctrl+C
    while (1) { // 
         printf("~\n");
         // 
    }
    return 0;
}
```

-  while while 

```c showLineNumbers
#include <stdio.h>
int main(){
    // Ctrl+C
    while (1)  // 
         printf("~\n");
         // return
    return 0;
}
```

-  while 

```c showLineNumbers
#include <stdio.h>
int main(){
    // while
    // 
    /*
    while (1)
         int num = 10; // 
    */
    
    // 
    while (1) {
        int num = 10; // 
        printf("num = %i\n", num);
        break; // break
    }
    return 0;
}
```

<Highlight>C ;</Highlight>

```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 0;
    
    // 
    // while (count < 3); while
    // while
    
    /*
    while (count < 3); // 
    {
        printf("~\n");
        count++;
    }
    */
    
    // 
    while (count < 3){ // 
        printf("~\n");
        count++;
    }
    return 0;
}
```

- 

```c showLineNumbers
#include <stdio.h>
int main(){
    // Ctrl+C
    // 
    while (1); // 
    
    printf("\n");
    return 0;
}
```


### do-while

- 

```c showLineNumbers
do {
    ;
    ;
    ....
} while ( );
```

- 

```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 0;
    do {
        printf("~\n");
        count++;
    }while(count < 10);
    return 0;
}
```

- do-while 

  -  while ""
  -  while 
  - ""

- 
  - 

```c showLineNumbers
#include <stdio.h>
int main()
{
    int num = -1;
    do{
        printf(",\n");
        scanf("%d", &num);
    }while(123456 != num);
    printf("\n");
    return 0;
}
```

- **do-while **
  - do-while 

```c showLineNumbers
#include <stdio.h>
int main(){
    // do-whileCtrl+C
    do {
        printf("\n");
    } while(1);
    return 0;
}
```

- while  do-while 
  -  while  do-while  while  while
  -  do-while
  - do-while 

### for

- 

```c showLineNumbers
for(; ; ) {
    ;
}
```

- 

```c showLineNumbers
#include <stdio.h>
int main(){
    for(int i = 0; i < 10; i++){
        printf("~\n");
    }
    return 0;
}
```

- for 

  - ""****
  - ""
  - ""
  -  for 

- for 

  -  while 
  -  `for(;;);`

```c showLineNumbers
#include <stdio.h>
int main(){
    // forCtrl+C
    for(;;); // 
    
    printf("\n");
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>
int main(){
    // forCtrl+C
    for(;;){
        printf("\n");
    }
    return 0;
}
```

- for  while 
  - while  for  for  for for 
  -  while  for 

```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 0; // 
    while (count < 10) { // 
        printf("~ %i\n", count);
        count++; // 
    }
    // , , while
    printf("count = %i\n", count);
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>
int main(){
    // : for, for{}
    // : , , for
    // ,  , while
    for (int count = 0; count < 10; count++) {
        printf("~ %i\n", count);
    }
    // printf("count = %i\n", count); // countfor
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>
int main(){
    // , 
    int count = 0;
    for (; count < 10; count++) {
        printf("~ %i\n", count);
    }
    printf("count = %i\n", count);
    return 0;
}
```

## 

- C  returnbreakcontinuegoto

### break

-  switch 

```c showLineNumbers
switch(){
    case 1:
        break;//switch
};//switch

while(){
    break;//while
};// while
for(){
    break;//for
};// for
```


- **break **
  - break 

```c showLineNumbers
#include <stdio.h>
int main(){
    // if(1) {
    //   break; // breakswitch
    // }
    return 0;
}
```

-  break 

```c showLineNumbers
#include <stdio.h>
int main(){
    int outer_count = 0;
    while(outer_count < 3) {
        int inner_count = 0;
        while(inner_count < 2) {
            printf("\n");
            break;// , 
        }
        printf("\n");
        outer_count++;
    }
    return 0;
}
```

- break 

```c showLineNumbers
#include <stdio.h>
int main(){
    int count = 0;
    while(count < 2){
        break;
        printf("!\n");// break
    }
    return 0;
}
```

### continue

- continue 

- ********

```c showLineNumbers
#include <stdio.h>
int main(){
    int i = 0;
    while (i < 10) {
        i++;
        if (i == 5) {
            // i5
            // i5i = 5
            continue;
        }
        printf("i = %d\n", i);
    }
    return 0;
}
```

- **continue **
  - continue 

```c showLineNumbers
#include <stdio.h>
int main(){
    // if(1) {
    //   continue; // continue
    // }
    return 0;
}
```

### return

-  return 

```c showLineNumbers
#include <stdio.h>
int main(){
    printf("\n");
    while (1) {
        while(1){
            printf("\n");
            // main
            return 0;
        }
        printf("\n");
    }
    printf("\n");
}
```

### goto

:::warning
goto 

goto 
:::

```c showLineNumbers
#include <stdio.h>

// 1
int main(){
    int outer = 0;
    while (outer < 3) {
        int inner = 0;
        while(inner < 3){
            printf(": %d, : %d\n", outer, inner);
            if(outer == 1 && inner == 1){
                // 
                goto end_loops;
            }
            inner++;
        }
        outer++;
    }
    //labelC goto 
    // :
end_loops:
    printf("\n");
    return 0;
}
```

```c showLineNumbers
#include <stdio.h>

// 2goto
int main(){
    int num = 0;
    // loop:label
loop:
    if(num < 10){
        printf("num = %d\n", num);
        num++;
        // goto looploop
        goto loop;
    }
    return 0;
}
```

