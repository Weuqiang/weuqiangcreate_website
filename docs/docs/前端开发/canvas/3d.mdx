# Three.JS
WebGLAPIThree.JSBabylonWebGL

## Geometry
Three.JSGeometryface indicesUV

``` js
const geometry = new THREE.BoxGeometry( 1, 1, 1 );
```

:::info face indices
face indices
:::

## Material
Three.JSMaterialWebGLProgramUniform

WebGLProgram`gl.drawArrays()`/Three.JSMaterialProgram
`gl.drawArrays()`GPU/Program

``` js
const material = new THREE.MeshBasicMaterial( { color: 0x00ff00 } );

```

## Mesh
MeshMeshLinePoint


``` js
const geometry = new THREE.BoxGeometry( 1, 1, 1 );
const material = new THREE.MeshBasicMaterial( { color: 0x00ff00 } );
const cube = new THREE.Mesh( geometry, material );

scene.add( cube );
```


## Camera
Three.JS

##  OrthographicCamera
``` js
const camera = new THREE.OrthographicCamera( width / - 2, width / 2, height / 2, height / - 2, 1, 1000 ); // leftrighttopbottomnearfar
scene.add( camera );
```

##  PerspectiveCamera
``` js
const camera = new THREE.PerspectiveCamera( 45, width / height, 1, 1000 ); // fovapsectzNearzFar
scene.add( camera );
```

## Light



- color
- ambient Lighting
- diffuse Lighting
- specular Lighting


GLSL/****`MeshPhongMaterial``shininess``MeshLambertMaterial``MeshBasicMaterial`



## Direction Light

## Point Light

## Spot Light


## 3DModelloader

## GLTF


