---
sidebar_position: 11
---
# a

## 

hyperlink

`<a>``<a>`



```html
<a href="https://wikipedia.org/"></a>
```

“”`href`

`<a>`

```html
<a href="https://www.example.com/">
  <img src="https://www.example.com/foo.jpg">
</a>
```

`<a>`

## 

`<a>`

### href

`href` URL 

 URL 

```html
<a href="#demo"></a>
```

`href``#``demo`

### hreflang

`hreflang`

```html
<a
  href="https://www.example.com"
  hreflang="en"
></a>
```

`href`

`hreflang``x-default`

```html
<a href="https://example.com" hreflang="x-default">English</a>
<a href="https://example.com/de" hreflang="de">German</a>
```

`hreflang``x-defalut`

`hreflang``lang``lang`

### title

`title`

```html
<a
  href="https://www.example.com/"
  title="hello"
></a>
```

`hello`

### target

`target``<iframe>`

```html
<p><a href="http://foo.com" target="test">foo</a></p>
<p><a href="http://bar.com" target="test">bar</a></p>
```

`test``foo``test``test``foo.com``bar``test``bar.com``foo.com`

`target`

- `_self`
- `_blank`
- `_parent``<iframe>``_self`
- `_top``_self`

```html
<a
  href="https://www.example.com"
  target="_blank"
></a>
```



`target``rel="noreferrer"`

### rel

`rel`

```html
<a href="help.html" rel="help"></a>
```

`rel`

`rel`

- `alternate`
- `author`
- `bookmark`
- `external`
- `help`
- `license`
- `next`
- `nofollow`
- `noreferrer` HTTP `Referer`
- `noopener` JavaScript `window.opener`
- `prev`
- `search`
- `tag`

### referrerpolicy

`referrerpolicy` HTTP `Referer`

`no-referrer``no-referrer-when-downgrade``origin``origin-when-cross-origin``unsafe-url``same-origin``strict-origin``strict-origin-when-cross-origin`

`no-referrer``Referer``same-origin``Referer``origin`++ HTTP 

### ping

`ping` POST 

```html
<a href="http://localhost:3000/other" ping="http://localhost:3000/log">
  Go to Other Page
</a>
```

`http://localhost:3000/log` POST 

 HTTP `ping-from``ping-to``href`

```http
headers: {
  'ping-from': 'http://localhost:3000/',
  'ping-to': 'http://localhost:3000/other'
  'content-type': 'text/ping'
  // ...other headers
},
```

`ping`Firefox 

### type

`type` URL  MIME 

```html
<a
  href="smile.jpg"
  type="image/jpeg"
></a>
```

`type`

### download

`download` URL

```html
<a href="demo.txt" download></a>
```



`download`

`download`

```html
<a
  href="foo.exe"
  download="bar.exe"
></a>
```

`foo.exe``bar.exe`

 HTTP `Content-Disposition``download`

`download``data:``download`

```html
<a href="data:,Hello%2C%20World!"></a>
```

`Hello World!`


```html
<a
  href="data:,Hello%2C%20World!"
  download="hello.txt"
></a>
```

`hello.txt`“Hello, World!”

## 

`mailto`

```html
<a href="mailto:contact@example.com"></a>
```

`contact@example.com`



- `subject`
- `cc`
- `bcc`
- `body`



```html
<a
  href="mailto:foo@bar.com?cc=test@test.com&subject=The%20subject&body=The%20body"
></a>
```

`cc``subject``body` URL `%20`



```html
<a href="mailto:"></a>
```

## 

`tel`

```html
<a href="tel:13312345678">13312345678</a>
```



