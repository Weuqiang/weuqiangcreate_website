---
sidebar_position: 6
title: 
---

C C , 

 C , C 

 C 



- **_:_**  C ,,, printfscanfgetcharputchar 
- **_:_**,,,



- **_:_** ,( return )
- **_:_** ,( void,  return )



- **_:_** 
- **_:_** ,(),()


## 

- 

```c showLineNumbers
 ( 1 2…) {
    ;
    ;
}
```
* 
* 
* 
* : 

- 

```c showLineNumbers
int max(int value1, int value2) {
    int result = value1 > value2 ? value1 : value2;
    printf("\n");
    printf("max = %i\n", result);
    return result;
}

void min(int value1, int value2) {
    int result = value1 < value2 ? value1 : value2;
    printf("\n");
    printf("min = %i\n", result);
}

void call(){
    printf("\n");
    //  return 
}

int main(){
    printf("\n");
    // Cmainint
    return 0;
}
```


## 

- 
  - ****()
  - ,,
  - ,,

```c showLineNumbers
int max(int number1, int number2) //  
{
    return number1 > number2 ? number1 : number2;
}
```

- 
  - **\*\***, 
  - ,,,,
  - ,

```c showLineNumbers
int main() {
    int num = 99;
    // 88, num, 
    max(88, num); // 
    return 0;
}
```


- 

  ```c showLineNumbers
  int max(int number1, int number2) { //  
      return number1 > number2 ? number1 : number2;
  }
  int main() {
      // 2, , 
      max(88); // 
      return 0;
  }
  ```

- , <Highlight></Highlight>

  ```c showLineNumbers
  void change(double number1, double number2) {//  
     // : 10.000000, 20.000000
     // double
     printf("number1 = %f, number2 = %f", number1, number2);
  }
  int main() {
      change(10, 20);
      return 0;
  }
  ```

- charintfloat <Highlight></Highlight>

  ```c showLineNumbers
  void change(int number1, int number2) { //  
      number1 = 250; // 
      number2 = 222;
  }
  int main() {
      int a = 88;
      int b = 99;
      change(a, b);
      printf("a  = %d, b = %d", a, b); // : 88, 99
      return 0;
  }
  ```

## 

-  int

  ```c showLineNumbers
  max(int number1, int number2) {//  
      return number1 > number2 ? number1 : number2;
  }
  ```

-  return  return 

  ```c showLineNumbers
  int max(int number1, int number2) {//  
      return number1 > number2 ? number1 : number2;
      printf(""); // 
      return 250; // 
  }
  ```

-  return ,<Highlight>,</Highlight>

  ```c showLineNumbers
  int height() {
      return 3.14;
  }
  int main() {
    double temp = height();
    printf("%lf", temp);// : 3.000000
  }
  ```

## 

, 



` ( 1 2…);`

:::tip


,, , , 

, 

:::

```c showLineNumbers
// 
void getMax(int v1, int v2);
void getMax(int v1, int v2);
void getMax(int v1, int v2); // 
int main(int argc, const char * argv[]) {
    getMax(10, 20); // 
    return 0;
}
// 
void getMax(int v1, int v2) {
    int max = v1 > v2 ? v1 : v2;
    printf("max = %i\n", max);
}
```

,, 

```c showLineNumbers
int main() {
    void getMax(int v1, int v2); // , 
    getMax(10, 20); // 
    return 0;
}
// 
void getMax(int v1, int v2) {
    int max = v1 > v2 ? v1 : v2;
    printf("max = %i\n", max);
}
```


## 

main  `demo`  `hello world`

`gcc demo.c -o demo && ./demo hello world`

 main 

```c showLineNumbers title="demo.c"
int main(int argc, const char * argv[]) {
    printf("argc = %d\n", argc); // 
    printf("argv = %s\n", argv[0]); // 1
    printf("argv = %s\n", argv[1]); // 2
    printf("argv = %s\n", argv[2]); // 3
    return 0;
}
/*
argc = 3
argv = ./demo
argv = hello
argv = world
*/
```

## 



```c showLineNumbers
void function(int x){
    function(x);
}
```


- ****
- ****
- ****

- 

* ,
* "", "",""
* (), , 


- :

* , 

```c showLineNumbers
void getNumber(){
    int number = -1;
    while (number < 0) {
        printf("\n");
        scanf("%d", &number);
    }

    printf("number = %d\n", number);
}
```

```c showLineNumbers
void getNumber2(){
    int number = -1;
    printf("\n");
    scanf("%d", &number);
    if (number < 0) {
//        
        getNumber2();
    }else{
//        
       printf("number = %d\n", number);
    }
}
```
