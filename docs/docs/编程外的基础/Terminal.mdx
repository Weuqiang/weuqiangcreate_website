---
sidebar_position: 4
title: Terminal
---

Terminal

:::info
 Unix  UbuntuDebianCentOSFedoraArch Linux 

 Ubuntu  Ubuntu 22.04 "Jammy Jellyfish"
:::

[ bash ](https://wangdoc.com/bash/intro) bash 

[tldr](https://tldr.inbrowser.app/)

## WSL

 Windows 10  2004  19041  Windows 11  <HoverText text="WSLWindows Subsystem for Linux" explanation=" Windows  Linux  Bash "/>

[WSL](https://learn.microsoft.com/zh-cn/windows/wsl/)

:::tip 
-  `wsl.exe`  `wsl`
- `wsl --unregister` 
:::

### 

|  |  |  |  |
|---------|---------|---------|------|
| WSLUbuntu | `wsl --install` | `wsl --install` | - |
|  | `wsl --install <DistributionName>` | `wsl --install Ubuntu-24.04` | - |
|  | `wsl --list --online` | `wsl --list --online` | `wsl -l -o` |
|  | `wsl --list --verbose` | `wsl --list --verbose` | `wsl -l -v`|
|  | `wsl --list --running` | `wsl --list --running` | `wsl -l --running` |
|  | `wsl --list --all` | `wsl --list --all` | `wsl -l --all`/ `wsl -l` |

### 
|  |  |  |  |
|---------|---------|---------|------|
| LinuxIP | `wsl hostname -I` | `wsl hostname -I` | - |
| WindowsIP | `wsl ip route show` | `wsl ip route show` | - |
| WSL | `wsl --version` | `wsl --version` | `wsl -v` |
| WSL | `wsl --status` | `wsl --status` | - |
|  | `wsl --help` | `wsl --help` | `wsl -h` |

### 
|  |  |  |  |
|---------|---------|---------|------|
|  | `wsl --set-default <DistributionName>` | `wsl --set-default Ubuntu-24.04` | `wsl -s Ubuntu-24.04` |
| WSL(12) | `wsl --set-version <DistributionName> <Version>` | `wsl --set-version Ubuntu-24.04 2` | - |
| WSL | `wsl --set-default-version <Version>` | `wsl --set-default-version 2` | - |
|  | `<DistributionName> config --default-user <Username>` | `ubuntu config --default-user johndoe` | - |

### 

|  |  |  |  |
|---------|---------|---------|------|
|  | `wsl` | `wsl` | - |
|  | `wsl --distribution <DistributionName>` | `wsl --distribution Ubuntu-24.04` | `wsl -d Ubuntu-24.04` |
|  | `wsl --user <Username>` | `wsl --user root` | `wsl -u root` |
|  | `wsl -d <DistributionName> -u <Username>` | `wsl -d Ubuntu-24.04 -u root` | - |
|  | `wsl ~` | `wsl ~` | - |

### 
|  |  |  |  |
|---------|---------|---------|------|
|  | `wsl --export <DistributionName> <FileName>` | `wsl --export Ubuntu-24.04 ubuntu-backup.tar` | - |
| VHD | `wsl --export <DistributionName> <FileName> --vhd` | `wsl --export Ubuntu-24.04 ubuntu-backup.vhdx --vhd` | - |
|  | `wsl --import <DistributionName> <InstallLocation> <FileName>` | `wsl --import Ubuntu-ROS2 C:\WSL_ROS2 ubuntu-backup.tar` | - |
| VHD | `wsl --import <DistributionName> <InstallLocation> <FileName> --vhd` | `wsl --import Ubuntu-New C:\WSL_New backup.vhdx --vhd` | - |
| VHD | `wsl --import-in-place <DistributionName> <FileName>` | `wsl --import-in-place Ubuntu-New ubuntu.vhdx` | - |

### 
|  |  |  |  |
|---------|---------|---------|------|
|  | `wsl --terminate <DistributionName>` | `wsl --terminate Ubuntu-24.04` | `wsl -t Ubuntu-24.04` |
|  | `wsl --shutdown` | `wsl --shutdown` | - |
|  | `wsl --unregister <DistributionName>` | `wsl --unregister Ubuntu-24.04` | - |
| WSL | `wsl --update` | `wsl --update` | - |
| GitHub | `wsl --update --web-download` | `wsl --update --web-download` | - |

### 
|  |  |  |  |
|---------|---------|---------|------|
|  | `wsl --mount <DiskPath>` | `wsl --mount \\.\PHYSICALDRIVE1` | - |
| VHD | `wsl --mount <DiskPath> --vhd` | `wsl --mount C:\disk.vhdx --vhd` | - |
|  | `wsl --mount <DiskPath> --partition <PartitionNumber>` | `wsl --mount \\.\PHYSICALDRIVE1 --partition 1` | - |
|  | `wsl --mount <DiskPath> --type <Filesystem>` | `wsl --mount \\.\PHYSICALDRIVE1 --type ext4` | `wsl --mount <DiskPath> -t <Filesystem>` |
|  | `wsl --unmount <DiskPath>` | `wsl --unmount \\.\PHYSICALDRIVE1` | - |
|  | `wsl --unmount` | `wsl --unmount` | - |


## 5

` [] []`


- `ls` - 
- `ls -l` - `-l` 
- `ls --list /home` - 

:::tip 
- ****`-l`
- ****`--list`
- `ls -l` = `ls --list`
:::

### 

|  |  |  |  |
|------|------|------|------|
| `;` |  | `cd /tmp; ls` |  |
| `&&` |  | `mkdir test && cd test` |  |
| `\|` |  | `ls -l \| grep "txt"` | txt |

**`|`**

```bash
# "python"
ps aux | grep python
```

### 

 `sudo`**su**per user **do**

```bash
sudo apt-get update      # 
sudo reboot              # 
```

****
- `sudo ` - 
- `sudo su` - root
- `exit` - root

## 



|  |  |  |
|--------|------|----------|
| `Tab` | / |   |
| `Ctrl + C` |  |  |
| `Ctrl + L` |  |  |
| `↑` / `↓` |  |  |
| `Ctrl + D` |  |  |
| `Ctrl + U` |  |  |
| `Ctrl + W` |  |  |

:::tip Tab
-  `cd /h` Tab →  `cd /home`
- Tab → 
- **Tab**
:::

## 

### 

|  |  |  |  |
|------|------|------|----------|
| `pwd` |  | `pwd` | **p**rint **w**orking **d**irectory |
| `ls` |  | `ls` / `ls -la` | **l**i**s**t |
| `cd` |  | `cd /home` | **c**hange **d**irectory |
| `mkdir` |  | `mkdir test` | **m**a**k**e **dir**ectory |
| `touch` |  | `touch file.txt` | touch |
| `rm` |  | `rm file.txt` | **r**e**m**ove |
| `rm -r` |  | `rm -r folder` | **r**e**m**ove **r**ecursive |
| `cp` |  | `cp a.txt b.txt` | **c**o**p**y |
| `mv` | / | `mv old.txt new.txt` | **m**o**v**e |
| `cat` |  | `cat readme.txt` | con**cat**enate |

:::danger
`rm -rf /` ****
- `-r`
- `-f`
:::

### 

**1nano**
```bash
nano file.txt          # 
# Ctrl+O 
# Ctrl+X 
```

**2vim**
```bash
vim file.txt           # 
#  i 
#  Esc 
#  :wq 
```

### 

Linux3`chmod  `

****
- **1**
- **2**
- **3**

****
- **4** = **r**ead
- **2** = **w**rite  
- **1** = e**x**ecute
- `7 = 4+2+1 = ++`

#### 

|  |  |  |  |
|--------|------|----------|------|
| `644` |  |  | `chmod 644 readme.txt` |
| `755` | + | / | `chmod 755 script.sh` |
| `600` |  |  | `chmod 600 ~/.ssh/id_rsa` |
| `777` |  |   | - |

****
```bash
ls -l file.txt
# -rw-r--r-- 1 user group 1024 Dec 15 10:30 file.txt
#       ↑ ↑ ↑
#       rw-    r-- r--
```

:::warning 
- **777**
- SSH600SSH
- `chmod +x script.sh`
:::

## 

### 

Ubuntu/Debian `apt` 

```bash
sudo apt update              # 
sudo apt install        # 
sudo apt remove         # 
sudo apt upgrade             # 
```

****
```bash
sudo apt update
sudo apt install python3-pip    # Pythonpip
sudo apt install git            # Git
```

### 

|  |  |  |
|------|------|----------|
| `top` |  |  `q`  |
| `htop` |  | `sudo apt install htop` |
| `df -h` |  | `-h`  |
| `free -h` |  | `-h`  |
| `ps aux` |  |  `grep`  |

### 



**WSL**

```bash
# 
export MY_VAR="hello"
echo $MY_VAR              # 

# 
echo 'export MY_VAR="hello"' >> ~/.bashrc
source ~/.bashrc          # 
```

****

|  |  |  |
|--------|------|------|
| `$HOME` |  | `/home/username` |
| `$PATH` |  | `export PATH=$PATH:/new/path` |
| `$USER` |  |  |
| `$PWD` |  |  |

:::tip Windows PowerShell
Windows PowerShell
- `$env:MY_VAR = "hello"`
- `echo $env:MY_VAR`
:::

### 

""

```bash
# 
lsof -i:8080                          # 8080
netstat -tunlp | grep 8080            # 

# 
kill -9 $(lsof -i tcp:8080 -t)        # 8080

# 
nohup python3 app.py &                # 
```

### SSH

SSHLinux

```bash
# 
ssh username@192.168.1.100            # 

# 
ssh -p 2222 username@example.com      # 22

# scp
scp local.txt user@host:~/            # 
scp user@host:~/remote.txt ./         # 
scp -r folder/ user@host:~/           # 
```

:::tip SSH

```bash
ssh-keygen                            # SSH
ssh-copy-id user@host                 # 
# 
```
:::

## Makefile

Makefile 

 `Makefile` 

```makefile showLineNumbers
# 
# : 
#	Tab

hello:
	echo "Hello World"

# 
clean:
	echo "clean1"
	echo "clean2"
	echo "clean3";echo "clean4"

# all  build  test
# make all  
#  make build 
#  make test 
#  echo "all"
all: hello clean
	echo "all"

# 
PYTHON = python3.12
# 
.PHONY: build clean
build:
	$(PYTHON) -v
```

`make hello` → `make clean` → `make all` → `make build`

 `make` 

 `build`  `make build`  `Makefile`  `build` 

 `.PHONY: 1 2`  `Makefile`  `1``2`


### make 



|  |  |  |  |
|------|------|------|------|
| `make` |  | `make` |  Makefile  |
| `make ` |  | `make hello` |  hello  |
| `make -f ` |  Makefile | `make -f Makefile` |  |
| `make -C ` |  | `make -C ./src` |  src  |
| `make -n` | dry run | `make -n build` |  |
| `make -s` |  | `make -s clean` |  |
| `make -j N` |  | `make -j 4` |  4  |
| `make -B` |  | `make -B` |  |
| `make -k` |  | `make -k test` |  |
| `make VAR=value` |  | `make PORT=8080 run` |  Makefile  |
