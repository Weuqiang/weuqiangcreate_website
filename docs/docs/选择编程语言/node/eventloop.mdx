# 

NodeNode `11.0`

GUIJavaScriptI/O

 `fetch`********

********************

`setTimeout` `setInterval``I/O` `ajax``setImmediate`Node `promise.then()`

...+ `tick` `tick`Node `process.nextTick` `Vue` `$.nextTick` `tick`

## nextTick

`process.nextTick`Node `tick` `process.nextTick` `nextTick`

Node `nextTick` `tick` `nextTick`

```js
// 
setTimeout(() => {
    console.log(1)
    process.nextTick(() => {
        console.log(2)
    })
}, 0)

new Promise((resolve) => resolve())
.then(() => {
    console.log(3)
    process.nextTick(() => {
        console.log(4)
    })
})

process.nextTick(() => {
    console.log(5)
    process.nextTick(() => {
        console.log(6)
    })
    setImmediate(function () {
        console.log(7)
    })
})

process.nextTick(function () {
    console.log(8)
    process.nextTick(() => {
        console.log(9)
    })
})
```

## Node

NodeNode****** `nextTick`**

1. `Timer`: `SetTimeout` `SetInterval`
2. `pending callback`: TCP
3. `idle, prepare`: 
4. `poll`: ****
5. `check`: `SetImmediate`
6. `close callback`:  `Socket.on("close")`

`Timer``poll``check`

`v11.0`NodeNode******Node**

Node

```js
setImmediate(function(){
    console.log(1);
    process.nextTick(function(){
        console.log(4)
    }) 
})
process.nextTick(function(){
    console.log(2)
    setImmediate(function(){
        console.log(3);
    })
})
```

1.  `setImmediate` `check`
2.  `process.nextTick` `nextTick` `nextTick`
3. **2**  `setImmediate` `check`
4.  `check`
5.  `check`**1** `nextTick`

Node

**Node**

Node******34**

**Node**

Node******43**
