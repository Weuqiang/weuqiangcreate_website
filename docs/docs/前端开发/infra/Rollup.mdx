`Webpack``Rollup`

``` bash
npm i -D rollup
npx rollup main.js --file bundle.js --format umd --name "myModule"
```

`Rollup``iife``cjs``umd``es`

``` js
// rollup.config.js
module.exports = {
    input: 'src/main.js',
    output: {
        file: 'bundle.js',
        format: 'es'
    },
    // 
    output: [
        {
            file: 'bundle.es.js',
            format: 'es'
        },
        {
            file: 'bundle.umd.js',
            format: 'umd'
        }
    ]
}
```






