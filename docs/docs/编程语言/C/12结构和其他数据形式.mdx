---
sidebar_position: 12
title: 结构和其他数据形式
description: "结构体的定义、初始化与嵌套，联合体与枚举的用法，内存布局与字节对齐规则，以及结构体在 Python-C 互操作中与类的映射方式"
---

## 学习目标

学完本章，你应该能够：

- 掌握结构体 `struct` 的定义、初始化、成员访问与内存对齐
- 理解联合体 `union`、枚举 `enum` 与 `typedef` 别名
- 能组织嵌套结构并作为函数参数传递

## 前置知识

在阅读下面的内容前，建议先掌握：

- [数组与指针](../8数组和指针)

## 核心概念

数组装「同类型」，结构体装「不同类型」——它是把现实实体（学生、坐标、数据包）映射成一块连续内存的工具。内存对齐让结构体大小往往大于各成员之和，这是性能与空间的权衡。

本章主线：
- 结构体：定义、初始化、`.`/`->` 访问
- 内存对齐与 `sizeof` 计算
- 联合体 `union` 与枚举 `enum`
- `typedef` 类型别名

## 结构体

结构体是C语言中组织相关数据的核心机制，在Python-C互操作中用于：
- 定义复杂的数据结构
- 与Python类进行映射
- 高效传递多个相关数据

### 基本结构体定义
```c
// 定义结构体
struct Person {
    char name[50];
    int age;
    float height;
};

// 使用typedef简化
typedef struct {
    char name[50];
    int age;
    float height;
} Person;

// 声明变量
Person p1;
Person *p2 = malloc(sizeof(Person));
```

### 结构体初始化和访问
```c
// 初始化
Person p1 = {"张三", 25, 175.5};
Person p2 = {.name = "李四", .age = 30, .height = 180.0};

// 访问成员
printf("姓名: %s\n", p1.name);
printf("年龄: %d\n", p1.age);

// 指针访问
Person *ptr = &p1;
printf("身高: %.1f\n", ptr->height);
```

### 结构体嵌套
```c
typedef struct {
    int year;
    int month;
    int day;
} Date;

typedef struct {
    char name[50];
    Date birthdate;
    float salary;
} Employee;

Employee emp = {
    .name = "王五",
    .birthdate = {1990, 5, 15},
    .salary = 8000.0
};
```

## 联合体

联合体允许在同一内存位置存储不同的数据类型，在Python-C互操作中用于：
- 节省内存空间
- 实现变体类型
- 处理网络协议数据

### 基本联合体
```c
union Data {
    int i;
    float f;
    char str[20];
};

union Data data;
data.i = 10;
printf("data.i: %d\n", data.i);

data.f = 3.14;
printf("data.f: %.2f\n", data.f);
```

### 带标签的联合体
```c
typedef struct {
    enum { INT, FLOAT, STRING } type;
    union {
        int i;
        float f;
        char str[20];
    } value;
} Variant;

Variant var;
var.type = INT;
var.value.i = 42;

if (var.type == INT) {
    printf("整数值: %d\n", var.value.i);
}
```

## 枚举

枚举提供有意义的名称来代替数字常量：

```c
typedef enum {
    RED,    // 0
    GREEN,  // 1
    BLUE    // 2
} Color;

Color c = GREEN;
if (c == GREEN) {
    printf("这是绿色\n");
}
```

## Python-C互操作中的应用

### 结构体与Python类的映射
```c
// C结构体
typedef struct {
    double x;
    double y;
} Point;

export Point* create_point(double x, double y) {
    Point *p = malloc(sizeof(Point));
    p->x = x;
    p->y = y;
    return p;
}

export double point_distance(Point *p1, Point *p2) {
    double dx = p1->x - p2->x;
    double dy = p1->y - p2->y;
    return sqrt(dx*dx + dy*dy);
}
```

```python
# Python包装类
from ctypes import Structure, c_double, CDLL, POINTER

class Point(Structure):
    _fields_ = [("x", c_double), ("y", c_double)]

lib = CDLL('./geometry.so')
lib.create_point.argtypes = [c_double, c_double]
lib.create_point.restype = POINTER(Point)
lib.point_distance.argtypes = [POINTER(Point), POINTER(Point)]
lib.point_distance.restype = c_double

# 使用
p1 = lib.create_point(1.0, 2.0)
p2 = lib.create_point(4.0, 6.0)
distance = lib.point_distance(p1, p2)
print(f"距离: {distance}")
```

### 复杂数据结构的传递
```c
// 图像数据结构
typedef struct {
    int width;
    int height;
    int channels;
    unsigned char *data;
} Image;

export void process_image(Image *img) {
    // 处理图像数据
    for (int i = 0; i < img->width * img->height * img->channels; i++) {
        img->data[i] = 255 - img->data[i]; // 反色处理
    }
}
```

## 内存布局和对齐

### 结构体大小和对齐
```c
#include <stdio.h>

typedef struct {
    char a;     // 1字节
    int b;      // 4字节
    short c;    // 2字节
} Example;

int main() {
    printf("Sizeof Example: %zu\n", sizeof(Example));
    printf("Offset of a: %zu\n", offsetof(Example, a));
    printf("Offset of b: %zu\n", offsetof(Example, b));
    printf("Offset of c: %zu\n", offsetof(Example, c));
    return 0;
}
```

### 手动内存对齐
```c
// 确保结构体按8字节对齐
typedef struct __attribute__((aligned(8))) {
    char a;
    int b;
    short c;
} AlignedExample;
```

## 最佳实践

1. **使用typedef**：简化结构体类型名称
2. **初始化所有字段**：避免未初始化数据
3. **考虑内存布局**：合理安排字段顺序减少填充
4. **文档化结构体**：说明每个字段的用途和单位
5. **验证输入数据**：在Python-C边界进行数据验证

## 常见陷阱

1. **内存对齐问题**：不同平台对齐要求可能不同
2. **字节序问题**：网络传输时需要注意字节序
3. **填充字节**：结构体中的填充字节可能导致数据不一致
4. **指针生命周期**：确保C中分配的内存在Python中正确管理

结构体和联合体是C语言中组织数据的强大工具，在Python-C互操作中尤为重要。正确使用它们可以创建高效、类型安全的数据接口。


## 小结

本章用结构体把零散数据打包成「对象」：定义、访问、`->` 与 `.` 的区别，以及内存对齐带来的大小玄学。联合体共享内存、枚举提升可读性、`typedef` 简化类型名，共同构成 C 的复合数据工具箱。

## 练习

动手检验一下自己：

1. 为什么 `struct {int; char; double;}` 的 `sizeof` 通常不是三者之和？
2. 结构体指针访问成员用 `->` 还是 `.`？什么场景下二者等价？
3. 联合体 `union` 与结构体在内存占用上有何本质区别？

## 示例

下面这段程序演示结构体的 `typedef` 别名、指定初始化、嵌套成员访问、枚举取值，以及用 `sizeof` 与 `offsetof` 观察内存对齐产生的填充字节。

```c
#include <stdio.h>
#include <stddef.h>

typedef struct {
    int year;
    int month;
    int day;
} Date;

typedef struct {
    char   name[16];
    Date   hire_date;
    double salary;
} Employee;

typedef enum { LEVEL_JUNIOR, LEVEL_SENIOR } Level;

typedef struct {
    char  a;   /* 1 字节 */
    int   b;   /* 4 字节，需要 4 字节对齐 */
    short c;   /* 2 字节 */
} Packed;

int main(void) {
    Employee e = {
        .name      = "Zhang",
        .hire_date = {2021, 7, 1},
        .salary    = 12500.0
    };
    Employee *p = &e;

    /* 变量用 . 访问，指针用 -> 访问 */
    printf("%s %d-%02d-%02d %.1f\n",
           p->name, p->hire_date.year, p->hire_date.month,
           p->hire_date.day, p->salary);

    Level lv = LEVEL_SENIOR;
    printf("level = %d\n", (int)lv);

    printf("sizeof(Packed) = %zu\n", sizeof(Packed));
    printf("offset a=%zu b=%zu c=%zu\n",
           offsetof(Packed, a), offsetof(Packed, b), offsetof(Packed, c));
    return 0;
}
```

在常见的 64 位平台上 `sizeof(Packed)` 输出 12 而不是 7，多出来的 5 个字节就是编译器为满足对齐要求插入的填充。

## 易错点

- 结构体之间可以整体赋值，但只要成员里有指针，那就是**浅拷贝**：两个结构体指向同一块堆内存，任意一方 `free` 之后，另一方立刻变成悬空指针。
- 数组成员不能用 `=` 赋值：`e.name = "Zhang";` 编译不过，必须用 `strcpy(e.name, "Zhang");`，并确认目标数组能装下含结尾符 `\0` 的长度。
- 混用 `.` 与 `->`：结构体变量用 `.`，结构体指针用 `->`。`p->x` 等价于 `(*p).x`，而 `*p.x` 会先取成员再解引用，含义完全不同。
- 用 `memcmp` 判断两个结构体是否相等是错的：填充字节的内容不确定，逻辑上完全相同的两个结构体也可能比出差异，应逐成员比较。
- 联合体只有「最后一次写入的成员」有效：写完 `u.f` 再读 `u.i`，读到的是浮点数位模式被重新解释后的整数；要安全区分，就在外层结构体里配一个 `type` 标签字段。
- 结构体作为函数参数是**按值传递**，会整体拷贝一份；大结构体应传 `const` 指针，既省掉拷贝开销，也能防止函数内部意外修改。

## 延伸阅读

- 上一篇：[`11文件输入输出`](../11文件输入输出)
- 下一篇：[`13位操作`](../13位操作)
- 返回 [C 语言总览](../)
- 对照阅读：[Go 语言](../../Go/)、[Rust 语言](../../Rust/)、[Python 语言](../../Python/)

