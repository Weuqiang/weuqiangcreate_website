
`pip`  Python Python  `pip`

 Python  `pip`   `pip` 

`ensurepip` <Highlight></Highlight>  Python  `pip` 


`ensurepip`  API 

## 



```bash showLineNumbers
#  pip  pip
python -m ensurepip

#  pip  ensurepip 
python -m ensurepip --upgrade
```

 `pip` 

```bash showLineNumbers
#  pip 
python -m ensurepip --root <dir>

#  pip 
python -m ensurepip --user
```

`pipX`  `pipX.Y`X.Y  Python  `pip3`  `pip3.14` `--altinstall`  `--default-pip` 

```bash showLineNumbers
#  pipX.Y  pipX
python -m ensurepip --altinstall

#  pip  pip 
python -m ensurepip --default-pip
```

:::warning
[Python 3.14 ](https://docs.python.org/zh-cn/3.14/library/ensurepip.html#module-ensurepip)  `--altinstall`  `--default-pip` 
:::

##  API

 `pip` `ensurepip` 

### `ensurepip.version()`

`ensurepip`, `pip` 

```python showLineNumbers
import ensurepip
print(ensurepip.version()) # : '25.2'
```

### `ensurepip.bootstrap()`

 `pip` 

```python showLineNumbers
import ensurepip

#  pip
ensurepip.bootstrap()

#  pip 
ensurepip.bootstrap(upgrade=True, verbosity=1)

#  pip  pipX.Y 
# ensurepip.bootstrap(root='/path/to/my/env', altinstall=True)
```

:::tip
[Python 3.14 ](https://docs.python.org/zh-cn/3.14/library/ensurepip.html#module-ensurepip) `bootstrap`  `sys.path`  `os.environ` 
:::

